# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: hierarchy-stress.spec.js >> Top-Down Hierarchy Stress Test & Race Conditions >> Crea una rama entera (Unidad -> Portafolio -> Grupo -> Producto) sin pérdida de Subgrids
- Location: __tests__\e2e\hierarchy-stress.spec.js:43:3

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: locator('#sandboxFrame').contentFrame().locator('#userHtmlFrame').contentFrame().locator('ion-list').filter({ hasText: 'Portafolio E2E' })
Expected: visible
Timeout: 15000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 15000ms
  - waiting for locator('#sandboxFrame').contentFrame().locator('#userHtmlFrame').contentFrame().locator('ion-list').filter({ hasText: 'Portafolio E2E' })

```

# Test source

```ts
  1   | const { test, expect, chromium } = require('@playwright/test');
  2   | 
  3   | let context;
  4   | let page;
  5   | 
  6   | test.describe('Top-Down Hierarchy Stress Test & Race Conditions', () => {
  7   | 
  8   |   test.beforeAll(async () => {
  9   |     // Usamos el Perfil Nativo Persistente para que Google no nos detecte como Bot Inyector
  10  |     context = await chromium.launchPersistentContext('.auth/chrome-profile', {
  11  |         headless: false,
  12  |         channel: 'chrome',
  13  |         args: [
  14  |             '--disable-blink-features=AutomationControlled',
  15  |             '--no-sandbox'
  16  |         ]
  17  |     });
  18  |     page = await context.newPage();
  19  |   });
  20  | 
  21  |   test.afterAll(async () => {
  22  |     await context.close();
  23  |   });
  24  | 
  25  |   test.beforeEach(async () => {
  26  |     // 1. Navegar al Endpoint Ejecutivo
  27  |     await page.goto('https://script.google.com/macros/s/AKfycbz1wK6yVfTuUe3kv35k45IULLBVya51t6HDXUZHNP-6rI9Nh_PEctWZseGyoQiQ2HxkIw/exec');
  28  |     
  29  |     // Si Google nos mandó al Login, pausamos amigablemente para que llenes tus credenciales
  30  |     if (page.url().includes('accounts.google.com')) {
  31  |         console.log("==============================================");
  32  |         console.log("ESPERANDO LOGIN MANUAL (Tienes 120 segundos)");
  33  |         console.log("==============================================");
  34  |         await page.waitForURL(/.*script\.google\.com.*/, { timeout: 120_000 });
  35  |     }
  36  | 
  37  |     // Esperar a que Taxonomia se hidrate dentro del doble iframe de Google Apps Script
  38  |     const frame = page.frameLocator('#sandboxFrame').frameLocator('#userHtmlFrame');
  39  |     await frame.locator('ion-app').waitFor({ state: 'visible', timeout: 60000 });
  40  |   });
  41  | 
  42  |   // Nota importante: Al usar nuestro propio "page", le decimos a Playwright que no inyecte el suyo ({ page }) 
  43  |   test('Crea una rama entera (Unidad -> Portafolio -> Grupo -> Producto) sin pérdida de Subgrids', async () => {
  44  |     const frame = page.frameLocator('#sandboxFrame').frameLocator('#userHtmlFrame');
  45  |     
  46  |     // =========================================================================
  47  |     // 1. CREAR UNIDAD DE NEGOCIO (Level 1)
  48  |     // =========================================================================
  49  |     await frame.locator('body').evaluate(() => {
  50  |         window.renderForm('Unidad_Negocio', {});
  51  |     });
  52  |     
  53  |     // Fallback if Ionic 8 doesn't expose native input gracefully:
  54  |     await frame.locator('body').evaluate(() => {
  55  |         const i = document.querySelector('ion-input[name="nombre"]');
  56  |         if (i) i.value = 'UN E2E Autofocus ' + Date.now();
  57  |     });
  58  | 
  59  |     // =========================================================================
  60  |     // 2. STRESS TEST: Crear Portafolios simultáneamente desde el Subgrid
  61  |     // =========================================================================
  62  |     await frame.locator('body').evaluate(() => {
  63  |         const headers = Array.from(document.querySelectorAll('strong')).filter(s => s.textContent.includes('Portafolios Asociados'));
  64  |         if (headers.length > 0) {
  65  |             const btn = headers[0].parentElement.querySelector('ion-button');
  66  |             if (btn) btn.click();
  67  |         }
  68  |     });
  69  | 
  70  |     // Esperar a que aparezca el modal de subgrid
  71  |     const btnCreateNewPortafolio = frame.locator('#btn-create-new');
  72  |     await btnCreateNewPortafolio.waitFor({ state: 'visible' });
  73  |     await btnCreateNewPortafolio.click();
  74  | 
  75  |     // Ahora estamos en el segundo Drawer (Portafolio)
  76  |     await frame.locator('body').evaluate(() => {
  77  |         const inputs = document.querySelectorAll('ion-input[name="nombre"]');
  78  |         // El último (top stack del drawer) es el portafolio
  79  |         inputs[inputs.length - 1].value = 'Portafolio E2E ' + Date.now();
  80  |     });
  81  | 
  82  |     // Guardar Portafolio
  83  |     await frame.locator('body').evaluate(() => {
  84  |         const btns = Array.from(document.querySelectorAll('ion-button'));
  85  |         const savePort = btns.find(b => b.textContent && b.textContent.includes('Guardar Portafolio'));
  86  |         if (savePort) savePort.click();
  87  |     });
  88  | 
  89  |     // Mutex Wait Race Condition
  90  |     await page.waitForTimeout(2000);
  91  | 
  92  |     // =========================================================================
  93  |     // 3. ASSERT: El elemento recién agregado sigue visible en el Subgrid
  94  |     // =========================================================================
  95  |     const unSubgridList = frame.locator('ion-list').filter({ hasText: 'Portafolio E2E' });
> 96  |     await expect(unSubgridList).toBeVisible();
      |                                 ^ Error: expect(locator).toBeVisible() failed
  97  | 
  98  |     // Guardar UN
  99  |     await frame.locator('body').evaluate(() => {
  100 |         const btns = document.querySelectorAll('ion-button');
  101 |         const saveUN = Array.from(btns).find(b => b.textContent.includes('Guardar Unidad'));
  102 |         if (saveUN) saveUN.click();
  103 |     });
  104 | 
  105 |     await page.waitForTimeout(3000);
  106 |     console.log("Ruta Crítica Superada. El Multi-nivel Subgrid conservó su estado Optimista.");
  107 |   });
  108 | });
  109 | 
```