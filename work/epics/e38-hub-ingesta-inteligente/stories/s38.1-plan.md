# Tareas de Ejecución S38.1

## Tarea 1: Creación del Módulo Componente `UI_ETL_Modal`
- **Fichero:** `src/UI_ETL_Modal.client.js`
- Construir `window.UI_ETL_Modal = { present: function(...) }` con Ionic Framework puro (JS).
- Crear bloque central con campo de texto para URL (`ion-input`) y delegador a `onDriveSync(url)`.
- Crear bloque secundario para invocación a `input.click()` oculto (`onUploadCSV(event)`).

## Tarea 2: Inyección y Refactorización del Toolbar
- **Fichero:** `src/Index.html`
- Inyectar el tag `<?!= include('UI_ETL_Modal'); ?>` antes del Engine.
- **Fichero:** `src/UI_DataView_Toolbar.client.js`
- Eliminar por completo `<input type="file" ...>`. Modificar la firma general `buildHeader(...)` para aceptar simplificadamente `onPrimaryImportClick`.
- Desviar el botón de la nubecita hacia `onPrimaryImportClick`.

## Tarea 3: Orquestación del Controlador Central
- **Fichero:** `src/DataView_UI.client.js`
- Modificar el método de escucha: crear la fachada `_openETLModal()` que invocará a `window.UI_ETL_Modal.present()`.
- Enganchar los Callbacks internos (por ahora emitirán un `console.log` o Tostada en pre-implementación asíncrona de las historias S38.2 y S38.3).
