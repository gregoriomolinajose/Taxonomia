# Scope: S38.4 Tolerancia a Inconsistencias y Chunking ETL

## In Scope
- Creación de función `processPayload` en `DataEngine_ETL.client.js` para fragmentar arreglos pre-parseados (simétrico a `processFile`).
- Sanitización de propiedades (Omitir Cabeceras de Auditoría) eliminando llaves que comiencen con `sys_`, u omitiéndolas del mapping dictado por `Schema_Engine`.
- Enlazar la terminación exitosa de `etl_extract_sheet_data` en `DataView_UI` directo al envío en red (chunking).
- Desplegar el mismo Modal asincrónico progresivo de CSV para las Extracciones de Drive.

## Out of Scope
- Hardening activo para esquivar duplicados en memoria BD comparando el `email` de Persona. Eso se abordará en S38.5.
- Interfaz gráfica masiva en tabla para que el usuario borre tuplas a mano; es automatizado y zero-touch.

## Done Criteria
- [ ] Pegar un Drive Sheet, cargarlo, y que la pantalla bloquee mientras inserta en lotes de 50.
- [ ] Omitir variables intocables por gobernanza (Ej: Ninguna columna `sys_createdAt` debe corromper el sistema original de tiempos).
