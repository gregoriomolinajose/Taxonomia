# Plan de Trabajo: S38.2

- `[ ]` 1. Actualizar array OAuth en `src/appsscript.json` aportando alcance local y permisología de Drive (Si Aplica).
- `[ ]` 2. Crear `src/Engine_ETL.gs` estableciendo el servicio `generateDriveTemplate`.
- `[ ]` 3. Vincular `Engine_ETL` con las dependencias del motor de Schema para derivar el arreglo de headers.
- `[ ]` 4. Registrar caso `'etl_generate_template'` en `API_Universal.gs` para conectar con el dispatcher backend.
- `[ ]` 5. Actualizar el callback frontal (`DataView_UI.client.js` en función `onGenerateTemplate`) para inyectar la promesa DataAPI y poblar el input nativo con la URL devuelta.
