# Scope S62.2: Migrate Dominio Dynamic Topology

## In Scope
- Implementing a business interceptor for `Dominio` in the backend.
- Migrating the mathematical calculation to deduce `relaciones_padre` from `orden_path`.
- Removing the equivalent legacy logic from the frontend ETL script.

## Out of Scope
- Modifying the underlying data structure or schema for `Dominio`.
- Migration of other entity-specific ETL rules (handled in other stories).

## Done Criteria
- [ ] The `Dominio` business interceptor successfully calculates `relaciones_padre` in the backend.
- [ ] The calculation precisely matches the logic previously executed in the frontend.
- [ ] The frontend logic in `DataEngine_ETL.client.js` (or similar) is removed.
- [ ] Tests verify the new backend interceptor logic handles valid and edge cases.
