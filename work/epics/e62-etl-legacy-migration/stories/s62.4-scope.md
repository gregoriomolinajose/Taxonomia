# Story S62.4: Clean up legacy ETL modules

## In Scope
- Identify all occurrences and references to `DataEngine_ETL.client.js` and `DataEngine_ETL_Capacidades.client.js`
- Delete the legacy client-side files (already deleted in previous commits but ensure clean slate in codebase if not).
- Remove `<script>` tags importing these files in HTML templates (e.g. `Index.html` or similar central templates).
- Verify that `UI_Component_BulkImporter.client.js` uses standard `window.DataEngine_ETL` fallback or backend orchestrator exclusively.
- Verify UI does not throw reference errors when starting an import.

## Out of Scope
- Modifying backend parser logic (done in S62.1 - S62.3)
- Modifying Job Worker mechanisms.

## Done Criteria
1. The codebase contains no references to `DataEngine_ETL.client.js` or `DataEngine_ETL_Capacidades.client.js`.
2. A generic import process successfully hits the backend pipeline without relying on legacy client-side parsers.
