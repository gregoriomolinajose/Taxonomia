# Scope: Migrate Persona Rules (Allowed Domains & Auto-Sync)

## In scope
- Migrate `Persona` `ALLOWED_DOMAINS` validation to the backend (`Business_Interceptors` or `hydrateAndDeduplicate`).
- Migrate `Persona` auto-provisioning (Workspace Sync) to trigger automatically after the ETL job completes (Job Queuing chaining).

## Out of scope
- Completely deleting `DataEngine_ETL.client.js` and `DataEngine_ETL_Capacidades.client.js` (will be done in S62.4).
- Modifying other entity rules like Dominio or Capacidades in the ETL (handled in S62.2, S62.3).

## Done when
- Persona validation is executed natively on the backend Job Worker.
- Workspace sync triggers reliably after Persona ETL processing.
- The legacy frontend implementations for these specific rules can be safely bypassed or removed.
- Tests (if applicable) confirm validation and job chaining work.
