# Scope: S15.2

## In Scope
- Remove `window.goToFormSection` from `UI_FormStepper.html`.
- Remove `window.getDominiosPadreOptions` alias from `UI_FormUtils.html`.
- Modify `FormRenderer_UI.html` to consume `UI_FormStepper` and `UI_FormUtils` using their namespaced `window.UI_Form...` properties.
- Purge redundant `setTimeout` instances mimicking GC, switching to Ionic's `ionModalDidDismiss` strictly where missing for safety.

## Out of Scope
- Rewriting the ModalManager stack entirely.
- Refactoring DataGrid logic.

## Done Criteria
- [ ] Developer tools console does not list `goToFormSection` under `window` scope.
- [ ] Developer tools console does not list `getDominiosPadreOptions` under `window` scope.
- [ ] Modals gracefully destruct without race-conditions.
