# Scope: S15.1

## In Scope
- Modifying `deploy.js` to install and run `acorn`.
- Modifying `Index.html` to inject a `window.onerror` UI fallback.
- Modifying `UI_FormSubmitter.html` to normalize `.trim()` checks.

## Out of Scope
- Full minification of JS using Rollup/Esbuild (we only AST validate).
- Refactoring the entire FormSubmitter (only fixing the empty string Truthiness Trap).

## Done Criteria
- [ ] Intentional syntax error is caught by `deploy.js` instead of being deployed to clasp.
- [ ] Manual intentional crash in frontend triggers the `Index.html` error boundary UI.
- [ ] Submitting empty spaces ("   ") is detected as `""` and triggers the form required fields rejection.
