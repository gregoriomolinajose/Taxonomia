# S41.2 Implementation Plan: Internal Hybrid State Engine

## Tareas

- `[ ]` **Tarea 1: El UI "Trigger" Base (Combobox Field)**
  - Dentro de `_render()`, eliminar el layout Dummy de la historia anterior.
  - Implementar la inyección estructurada de un tag `<ion-input>` en formato `floating label`.
  - Crear la lógica de la "Lupa" dinámica (Slot Box) y asignar su diseño (Icono dependiente de la entidad o neutro).
  - Integrar la inferencia `isMobile() => window.innerWidth <= 768;` para proteger al componente fijando modo "Readonly" del input nativo en celulares (lo que fuerza que el teclado numérico de iOS no salte antes del Modal).

- `[ ]` **Tarea 2: Gestor Lógico de Eventos del Trigger**
  - Suscribir `onclick`, `ionInput`, `ionClear` y `ionBlur`.
  - Inyectar la Bandera Anti-Carrera de Evento (`temporaryBlurFlag`) que migraremos íntegramente desde `SearchableSingle` para prevenir bugs de Blur vs Clicks en el combobox de Desktop.

- `[ ]` **Tarea 3: Componente Dropdown Nativo (Popover / Modal)**
  - Construir función `executeSearchAndOpen()`. Evaluar el booleano `isMobile()`.
  - Si es escritorio: Instanciar `<ion-popover size="cover">` delegando a Ionic el cálculo de redimensionamiento nativo. Atrás quedó el espagueti de las subgrillas legacy.
  - Si es móvil: Instanciar `<ion-modal>` (Bottom Sheet) con breakpoints nativos `[0, 0.5, 0.85, 1]` y cabecera de `<ion-searchbar>`.

- `[ ]` **Tarea 4: Renderizador Asíncrono de Lista Condicional**
  - Escribir `buildListItems()`. Iterar el arreglo `this._dataSource` limitado a N resultados basándose en el Typeahead activo.
  - Diseñar el switch de mapeo semántico del ítem:
    - Extracción genérica resguardada de llaves: `id_numero || valueField || id_registro || id`.
  - Diferenciar inyección de nodo:
    - *Single*: `<ion-item>` con Icono de "check" si corresponde al Selected. `onclick()` despacha Cierre.
    - *Multi*: Inyectar explícitamente `<ion-checkbox slot="end">`. `onclick()` solo conmuta (Toggle) el String del ID en el entorno `new Set()`, y re-dispara `buildListItems` para repintar.

## Verificación Local
- Confirmar fluidez de apertura del Modal en PWA.
- Validar las condiciones de exclusión (Multi almacena Set vivo y visualiza checkboxes; Single expulsa Payload string al instante de hacer tap).
