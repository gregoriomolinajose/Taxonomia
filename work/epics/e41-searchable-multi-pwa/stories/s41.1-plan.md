# S41.1 Implementation Plan: API Contract

## Tareas

- `[ ]` **Tarea 1: Estructura del Customelement**
  - Crear desde cero un archivo limpio `src/UI_Component_TXSearchable.client.js`.
  - Importarlo y agregarlo a `appsscript.json` si es requerido o confirmar integración en bundle.
  - Generar la clase `TXSearchable` que hereda de `HTMLElement`.
  - Añadir el `customElements.define('tx-searchable', TXSearchable);`.
  - Generar y amarrar un dummy shadow o Light DOM básico (`this.innerHTML = "Initializing Component..."`).

- `[ ]` **Tarea 2: Propiedades Nativas (Getters/Setters)**
  - Implementar la matriz de atributos `static get observedAttributes() { return ['entity-name', 'multiple', 'pre-selected', 'disabled']; }`.
  - Crear la firma `attributeChangedCallback(name, oldValue, newValue)` para capturar mutaciones en vivo y desencadenar un método interno vacío `_render()`.
  - Declarar las APIs puras de JavaScript `get/set dataSource` y `getValidatedValue()` arrojando un proxy hacia el state.

- `[ ]` **Tarea 3: Clean Architecture / Garbage Collection Hooks**
  - Declarar `connectedCallback()` y `disconnectedCallback()`.
  - Instanciar en `disconnectedCallback` una macro para destruir posibles timers fantasma, borrar colecciones del `dataSource` para purgar Garbage Collection y remover referencias.

- `[ ]` **Tarea 4: Interfaz de Comunicación Dummy**
  - Crear el helper en la base genérica `this.dispatchSelection()` que compile y dispare nativamente `new CustomEvent('txChange', { detail: { value: state.selection, multiple: this.multiple }})`.

## Verificación Local
- Simular un DOM de prueba o invocar manualmente `document.createElement('tx-searchable')`.
- Instanciarlo en Memoria Viva, setear sus atributos, confirmar que el `getValidatedValue()` responde al contrato acordado en E35 y despachar/recibir el `CustomEvent`.
- Ninguno de estos pasos tocará el Motor real para mantener esta Story atómicamente separada.
