# Plan S48.5: ETL Relational Data Validation & Provisioning

## Task 1: Habilitar Campos Relacionales en Plantilla ETL
- **File:** `src/Engine_ETL.js`
- **Action:** Modificar el `forEach` de `generateDriveTemplate` para que los campos de tipo `relation` **sí** sean incluidos en los headers de la hoja principal, en lugar de ser descartados.

## Task 2: Inyectar Pestaña de Catálogos (Sheet Secundario)
- **File:** `src/Engine_ETL.js`
- **Action:**
  1. Instanciar `Engine_DB.list('Cargo')` y `Engine_DB.list('Rol')`.
  2. Crear un nuevo sheet oculto en `ss` llamado `_Catalogos`.
  3. Pegar la lista de nombres de Cargos en la Columna A y los nombres de Roles en la Columna B.

## Task 3: Aplicar Data Validation a la Plantilla
- **File:** `src/Engine_ETL.js`
- **Action:**
  1. Identificar en qué columnas quedaron `id_cargo` y `roles_asignados`.
  2. Aplicar `SpreadsheetApp.newDataValidation().requireValueInRange()` referenciando `_Catalogos!A:A` para el cargo.
  3. Para `roles_asignados`, añadir una regla de validación de texto libre o un comentario a la cabecera (usando `setNote`) indicando el formato múltiple separado por comas.

## Task 4: Extender Interceptor de Negocio para Cargos
- **File:** `src/Business_Interceptors.js`
- **Action:** Asegurarse de que `AutoProvisionCargo` procese correctamente el `id_cargo` si lo que le llega es un nombre de Cargo desde la carga masiva (el Dropdown insertará nombres, no UUIDs). Ya que el interceptor busca tanto por nombre como por id externo, debemos validar que funcione en `matchRecordFn`.

## Task 5: Crear Lógica de Interceptor para Roles (Múltiple)
- **File:** `src/Business_Interceptors.js` o lógica anidada en `hydrateAndDeduplicate`.
- **Action:** Crear lógica que lea `roles_asignados` (texto separado por comas), lo divida, busque el UUID de cada rol, y programe las inserciones de las aristas `PERSONA_ROL` en la transacción.

## Task 6: Despliegue y Validación
- **Action:** Ejecutar npm run deploy:dev:auto, generar una plantilla real desde la UI y probar la ingesta completa con Dropdowns y múltiples roles.
