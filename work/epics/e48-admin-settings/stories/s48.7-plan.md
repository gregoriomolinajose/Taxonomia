# S48.7: Schema-Driven Relational Provisioning (Plan)

## Tasks

- [x] **T1: Actualizar Schema de Persona (`Schema_Engine.js`)**
    - Eliminar `AutoProvisionRoles` y `AutoProvisionEquipos` del array `mutationInterceptors`.
    - Añadir la propiedad `relationalProvisioners` con la configuración exacta (field, targetEntity, idField, edgeType, stubPrefix, extraStubFields) para "Roles" y "Equipos".
- [x] **T2: Limpiar Wrappers (`Business_Interceptors.js`)**
    - Borrar las funciones `AutoProvisionRoles` y `AutoProvisionEquipos` del objeto `INTERCEPTORS`.
- [x] **T3: Actualizar Enrutador (`Business_Interceptors.js`)**
    - Modificar la función `apply(entityName, items)` para leer la nueva propiedad `schema.relationalProvisioners`.
    - Añadir un bucle que invoque recursivamente `_provisionRelationalStubs(entityName, items, config)` para cada elemento de la configuración.
- [x] **T4: Despliegue y Pruebas DEV**
    - Ejecutar `npm run deploy:dev:auto`.
    - Realizar prueba de ingesta manual/backend para validar la creación correcta de relaciones M:N.
- [x] **T5: Despliegue a PROD**
    - Ejecutar `npm run deploy:prod:auto` una vez que DEV sea exitoso.
