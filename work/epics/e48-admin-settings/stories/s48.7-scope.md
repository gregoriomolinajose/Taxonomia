# S48.7: Schema-Driven Relational Provisioning (Scope)

## In Scope
- Agregar atributo de configuración `relationalProvisioning` o similar al esquema de la entidad `Persona` en `Schema_Engine.js`.
- Modificar el router de `Business_Interceptors.apply()` para leer esta configuración del esquema e invocar directamente a `_provisionRelationalStubs`.
- Eliminar las funciones `AutoProvisionRoles` y `AutoProvisionEquipos` de `Business_Interceptors.js`.
- Mantener la exportación de `_provisionRelationalStubs` o su accesibilidad en el scope correcto para que el router lo invoque de manera centralizada.

## Out of Scope
- Migrar lógicas recursivas (como `AutoProvisionLiderDirecto` o `AutoProvisionCargo`) al nuevo motor, ya que estas no encajan directamente en la lógica genérica de `_provisionRelationalStubs` sin extenderla significativamente.
- Cambios en el frontend o vistas UI.

## Done When
- [x] La configuración de interceptores M:N vive 100% en `Schema_Engine.js`.
- [x] Las funciones pasacables (wrappers) han sido borradas de `Business_Interceptors.js`.
- [x] La ingesta de pruebas confirma que roles y equipos M:N se aprovisionan correctamente bajo el nuevo router.
- [x] El código está desplegado a DEV y validado, y luego a PROD.
