# Scope: S53.1 Schema Simplification

## In Scope
- Modifying `APP_SCHEMAS.Taxonomia` in `Schema_Engine.js`.
- Removing relational configuration for `Portafolio` and `Grupo_Productos` inside the `Taxonomia` schema.
- Ensuring the Stepper UI automatically adjusts to the remaining sections.

## Out of Scope
- Building the UI Canvas (belongs to S53.2).
- Updating the FormEngine backend parser (should be resilient enough to handle missing fields).

## Done Criteria
- [ ] Schema updated.
- [ ] Taxonomia form renders properly with fewer fields.
- [ ] E2E save successful for a new draft Taxonomia with only the root entity.
