# Scope: S53.2 Visual Swimlane Layout

## In Scope
- Creating `UI_View_SwimlaneGrid.client.js`.
- Creating `CSS_TaxonomyCanvas.html`.
- Registering routes in `UI_Router.client.js`.
- Adding action buttons to Taxonomia Datagrid (`SelfService_Home_UI.client.js` or wherever Taxonomias are listed).
- Fetching and parsing `Sys_Graph_Edges`.
- Rendering static nodes (Unidad -> Portafolio -> Grupo).

## Out of Scope
- Making the nodes interactive (clicking `[+]` to add nodes). This is explicitly deferred to S53.3 to keep this story focused solely on the "Read/Render" skeleton.

## Done Criteria
- [ ] Route `#taxonomia/canvas/:id` loads correctly.
- [ ] The CSS layout mimics the nested HTML prototype (flex column -> flex row -> flex column).
- [ ] Nodes are colored using `APP_SCHEMAS[targetEntity].metadata.color`.
- [ ] The Canvas accurately displays existing data (if any edges exist for the selected Taxonomy).
