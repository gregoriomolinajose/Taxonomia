# S37.7 Plan: Refactorización de IoC en UI_DataGrid

## Tareas

- [ ] **Módulo 1: Refactorización de Orquestador DataView_UI**
  - Ubicar iteración de `window.UI_DataGrid.buildLayout` en `DataView_UI.client.js`.
  - Reemplazar todas las asignaciones `onMethod: 'window...'` por lambdas cerradas con funciones locales (`_onSort`, `_onSelectAll`, etc.).
  - Preservar integraciones de los métodos drag&drop extrayéndose si es necesario al path `window.DataView_DragDrop.onDrag`.

- [ ] **Módulo 2: Refactorización del DataGrid Presentacional**
  - Remover método `_invoke(callbackStr)` explícitamente en `UI_DataGrid.client.js`.
  - Cambiar sintaxis `this._invoke(this.cfg.onMethod, ...args)` a inyección condicional pura `if (typeof this.cfg.onMethod === 'function') this.cfg.onMethod(...args)`.
  - Asegurar que todas las 16 ocurrencias previas asociadas a `invoke` en la cuadricula sean adaptadas para tablas (`_renderTableView`) y tarjetas (`_renderGridView`).

- [ ] **Módulo 3: Quality Assurance**
  - Validar suite de UI Testing (Vitest + DOM Polyfill) en `DataGrid_Integration.ui.test.js`.
  - Asegurar nulas regresiones visuales (Coverage).
