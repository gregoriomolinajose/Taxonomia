# S37.6 Implementation Plan: UI_Factory Drawer Header

## Task Breakdown

### Phase 1: Módulo UI_Factory
- `[ ]` **1.1. Crear archivo `src/UI_Factory.client.js`.**
  - Implementar el Namespacing de `window.UI_Factory`.
  - Crear el método base `buildDrawerHeader(config)` con extracción y estabilización de constantes estéticas.
- `[ ]` **1.2. Mapeo de Layout.**
  - Copiar la lógica de flex-columns, topRow, Avatar e IdentityRow de `FormRenderer_UI.client.js`.
  - Conectar los argumentos paramétricos (`entityName`, `data`) al layout de Schema Utils existente y evitar llamadas duras acopladas al Controller.

### Phase 2: Inyección de Google Apps Script
- `[ ]` **2.1. Actualizar `src/Index.html`.**
  - Cargar `<?!= include('UI_Factory.client.js'); ?>` superior a `FormRenderer_UI.client.js` para certificar la resolución a tiempo del Namespace `UI_Factory` en el contexto global nativo.

### Phase 3: Acoplamiento a Controladores y Depuración de Código Viejo
- `[ ]` **3.1. Reemplazo de lógica en `src/FormRenderer_UI.client.js`.**
  - Remover la lógica del contenedor `header` (`lines 90-230`) del renderizado principal `openFormDrawer()`.
  - Insertar llamada `UI_Factory.buildDrawerHeader` y colgar su respuesta sobre el nodo DOM target correspondiente.

### Phase 4: Assurance
- `[ ]` **4.1. Correr Validaciones JSDOM.**
  - Ejecutar `npx vitest run __tests__/FormEngine_UI.ui.test.js`.
- `[ ]` **4.2. Commit final e Inserción de la Story.**
