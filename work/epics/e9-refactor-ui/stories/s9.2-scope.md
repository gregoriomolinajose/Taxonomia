# Scope: S9.2

## In Scope
- Creación de \`window.ModalStackController\` (o similar) para gobernar el enrutamiento visual.
- Límite topológico configurable (\`MAX_MODAL_DEPTH = 3\`) dentro de FormEngine.
- Limpieza de variables globales \`global.formModalStack\` y \`global.currentFormModal\` a setters/getters estructurados.
- Event Listener / Garbage Collection Guard que asegure que al cerrar un \`ion-modal\`, los nodos HTML se borren del DOM totalmente (\`modal.remove()\`).

## Out of Scope
- Backend o bases de datos temporales (TDAG).
- Refactorización del propio DataView (foco 100% en Modales / FormEngine).

## Done Criteria
- [ ] No existen referencias físicas a \`window.formModalStack\` para escritura directa.
- [ ] Intentar abrir 4 modales anidados es rechazado por el cliente con Alert proactivo.
- [ ] Profiling conceptual asegurado: cerrar un modal es un *Drop* perfecto de RAM (DOM node destruction explícita y test manual documentado).
