# Implementation Plan: S49.5 Schema Completo y Testing End-to-End

## Tasks

1. **Inyectar Esquema en `Schema_Engine.js`**
   - Localizar el objeto `APP_SCHEMAS`.
   - Agregar el registro `Wizard_Taxonomia`.
   - Declarar `formMode: "stepper"`, `form_stepper_stateful: true`.
   - Definir 3 secciones lógicas con los campos `nombre` (General), y múltiples campos relacionales (`M:N`) apuntando a: `Dominio`, `Capacidad`, `Unidad_Negocio`, `Familia`.

2. **Validación de Relaciones**
   - Asegurar que los nombres lógicos de los campos coincidan con el formato `rel_X` para M:N si se requiere, o nombres genéricos.
   - Definir `fields` ocultos estándar (`SYSTEM_FIELDS`, `id_registro`).

3. **Verificación Visual E2E**
   - Renderizar el portal y observar que el Stepper cargue dinámicamente el layout de las 3 secciones con sus correspondientes selectores (DataGrid embebidos para N:M).

4. **Despliegue a DEV**
   - Subir cambios para evaluación final de M3 (Feature Complete).
