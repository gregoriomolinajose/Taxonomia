# Plan: S49.6 Polish Visual (SaaS Premium UI)

## Task Breakdown

### 1. Inyección de Tokens de Diseño
- **Acción:** Actualizar `CSS_App.html` para incorporar los tokens faltantes que requería el diseño.
- **Detalle:** Añadir `--shadow-top` (para el efecto flotante del footer) y `--sys-font-heading` (para tipografía sin serifas).

### 2. Header y Container (Layout de Foco)
- **Acción:** Modificar `UI_Router.client.js` y `UI_FormStepper.client.js`.
- **Detalle:**
  - Sustituir el botón de cancelar por un ícono minimalista `close-outline`.
  - Configurar las grillas del wizard con `display: flex`, `justify-content: center`, `flex-direction: column`, `max-width: 600px` y `margin: 0 auto`.
  - Cambiar los estilos responsivos del Drawer Footer añadiéndole sombras.

### 3. Progreso Visual y Stepper Sidebar
- **Acción:** Refactorizar partes de `FormRenderer_UI.client.js` y `UI_FormStepper.client.js`.
- **Detalle:**
  - Instanciar `<ion-progress-bar>` anclado a la cima del footer.
  - Implementar etiqueta de texto de progreso "Paso X de Y".
  - Modificar los íconos de la barra lateral para utilizar avatares numéricos (1, 2, 3...) y coloridos.

### 4. Corrección Lógica del Checkmark y Chips en Buscador
- **Acción:** Mejorar `UI_FormStepper.client.js` y `UI_Component_TXSearchable.client.js`.
- **Detalle:**
  - Añadir soporte explícito a `tx-searchable` en la validación `recalculateAllStatuses`.
  - Modificar `_getSharedCardTemplate` de TXSearchable para utilizar píldoras estilo "Chip" (`flex-row wrap`) en vez de tarjetas en bloque.
