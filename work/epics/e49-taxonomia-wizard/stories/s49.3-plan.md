# Implementation Plan: S49.3 Cascarón Home de Negocio

## Tasks

1. **Creación del Componente de Vista (`SelfService_Home_UI.client.js`)**
   - Crear archivo nuevo en `src/`.
   - Definir `window.SelfService_Home_UI = { render: function(container) { ... } }`.
   - Inyectar estructura HTML premium (Hero Banner, text-gradients, CTA vibrante).

2. **Estilos Premium (`index.css`)**
   - Añadir clases para el Hero Banner (glassmorphism: `backdrop-filter: blur(10px)`).
   - Añadir animaciones (pulse en el botón, background con gradiente suave animado).
   - Asegurar que el layout cubra la pantalla completa correctamente.

3. **Invocación desde el Toolbar Administrativo (`UI_DataView_Toolbar.client.js`)**
   - Modificar el botón de utilidades o agregar un Action Button temporal/permanente en el Header que llame a `window.SelfService_Home_UI.render(mainViewportContainer)`.
   - Esconder el Sidebar Izquierdo administrativo cuando se entra al modo Self-Service (toggle de clases).
