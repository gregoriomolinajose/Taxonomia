# Implementation Plan: S49.2 Extensión UI Engine

## Tasks
1. **[FormRenderer_UI] Preparar IoC en renderForm**
   - Archivo: `src/FormRenderer_UI.client.js`
   - Buscar el lugar donde se invoca `DrawerStackController.push()`.
   - Modificar: Si `config.customContainer` existe, usarlo en su lugar y evadir el Drawer.
   - Ajustar lógica de cabeceras (`buildDrawerHeader`) si es necesario, o hacerla condicional.

2. **[UI_FormStepper] Incorporar modo Stateful**
   - Archivo: `src/UI_FormStepper.client.js`
   - Modificar constructor para aceptar `config.stateful`.
   - Crear método `_recalculateStatus(stepName)` que evalúe si los inputs dentro de esa sección tienen valor.
   - Escuchar eventos `change` o `ionChange` sobre `this.cardContent` delegadamente para actualizar íconos en tiempo real.

3. **Pruebas Locales (Mock)**
   - Correr manualmente el dev server o abrir en el framework existente para asegurar que los componentes no rompen el comportamiento "No-Stateful" y "Con-Drawer" (la ruta clásica administrativa debe seguir igual).
