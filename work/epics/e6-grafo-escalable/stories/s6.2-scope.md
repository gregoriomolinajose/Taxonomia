# Scope S6.2: Diccionario de Topologías

## In Scope
- Creación de constante `TOPOLOGY_DICTIONARY` en `Engine_Graph.js`.
- Reglas limitantes de cantidad de relaciones activas base-SCD2 (`maxActiveParents: 1` ó `null`).
- Manejo de excepción (`throw new Error`) durante la ingesta batch si se viola el cardinal.

## Out Scope
- UI Visual Warnings. (La API general de Engine_DB y el Router UI/Server de Apps Script arrojan Error como modal nativo cuando una promesa falla, apoyándonos en _Graceful Degradation_ arquitectónico).
