# Design S6.1: Config-Driven SCD-2

## Components Target
1. **Schema_Engine.gs**:
   - `Relacion_Dominios`: Añadir flag genérico para la red.
   - `Dominio.fields.relaciones_padre`: Como esto es un SUBGRID inyectado, la configuración viaja en la propiedad `relation` o `subgrid`. Injectar `isTemporalGraph: true` y `topology: "JERARQUICA_LINEAL"`.

2. **Engine_DB.js**:
   - Encontrar la función `orchestrateNestedSave`.
   - Reemplazar el bloque `if (targetEntity === "Relacion_Dominios")` por:
     ```javascript
     const schemaDef = APP_SCHEMAS[nodeType];
     const fieldDef = schemaDef && schemaDef.fields ? schemaDef.fields[subgridFieldId] : null;
     
     if (fieldDef && fieldDef.isTemporalGraph) {
         // Lógica SCD-2 automática (valido_hasta, etc)
     }
     ```

## Key Contracts
El Front-End enviará el payload idéntico. Cero impacto en el UI o red.

## Dependencies
Ninguna oculta. `Schema_Engine.gs` debe inicializarse antes que `Engine_DB.js` lo lea en App Script.
