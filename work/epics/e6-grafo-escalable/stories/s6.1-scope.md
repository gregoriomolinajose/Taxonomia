# Scope S6.1: Config-Driven SCD-2

## In Scope
- Agregar constantes `isTemporalGraph: true` y `topology: "JERARQUICA_LINEAL"` u otras básicas al `Schema_Engine.gs`.
- Extraer `targetEntity === 'Relacion_Dominios'` del `Engine_DB.js` en favor de lecturas de metadata de esquema.
- Pruebas E2E de inserción M:N.

## Out Scope
- Extracción a `Engine_Graph.js` (Esa tarea se hace en S6.2/S6.3). Esta historia S6.1 es netamente de Schema Engine Ingestion.
