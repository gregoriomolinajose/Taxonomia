# S13.2 Scope: Schema Declarative Push

## Goal
Centralizar toda la Lógica de Negocio y UI Lookups ("Hardcoded Strings") actualmente incrustados en los componentes Reactivos Angular-like, transicionándolos hacia el Motor Universal de Esquemas (`Schema_Engine.gs / APP_SCHEMAS`).

## Done Criteria
- `cant_*` y `total_integrantes` desaparece de `FormValidators.html` y `DataView_UI.html` siendo gobernado por una directiva `businessRules` en `APP_SCHEMAS`.
- El mapeo de Colores del DataGrid (`_badgeClass`) lee una matriz dinámica de `APP_SCHEMAS._UI_CONFIG.badgeMap` en vez de un Dictionary estático escondido.
- La SPA ya no frena el despliegue de Vistas Personalizadas.
