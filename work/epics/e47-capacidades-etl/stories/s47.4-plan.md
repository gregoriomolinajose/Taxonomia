# Plan S47.4: Implementación de Extracción Google Sheets para Capacidades

## 1. Modificación de Backend (`Engine_ETL.gs` y `API_Universal.js`)
- [x] Tarea 1.1: Agregar parámetro `options` a `extractDataFromDrive`.
- [x] Tarea 1.2: Implementar condicional para que devuelva `data` cruda si `options.rawMatrix` está activo.
- [x] Tarea 1.3: Modificar `etl_extract_sheet_data` en `API_Universal.js` para reenviar el objeto `options`.

## 2. Refactorización de Parser Capacidades (`DataEngine_ETL_Capacidades.client.js`)
- [x] Tarea 2.1: Migrar bloque de parseo lógico y aplanado a un nuevo método exportado `processMatrix`.
- [x] Tarea 2.2: Sustituir la lógica migrada dentro del onload de `processFile` para invocar `processMatrix` utilizando la respuesta de SheetJS.

## 3. Integración de UI (`DataView_UI.client.js`)
- [x] Tarea 3.1: En el flujo de importación remota (`onDriveSync`), agregar verificación particular para la entidad Capacidades.
- [x] Tarea 3.2: Llamar al endpoint `API_Universal_Router` agregando `{ rawMatrix: true }`.
- [x] Tarea 3.3: En el `.then()`, direccionar el payload resultante a `window.DataEngine_ETL_Capacidades.processMatrix`, suscribiéndose adecuadamente a callbacks y promesas.

## 4. Despliegue y Pruebas
- [x] Tarea 4.1: Desplegar a DEV y validación con el usuario.
- [x] Tarea 4.2: Desplegar a PROD y confirmación de enlaces web ejecutables.
