# Scope: S8.6 Relational Proximity Rules

## Boundaries

**In Scope:**
- Evolución de `filterAvailableOptions` en `SubgridState.js` para soportar `strictLevelJumps: false` (Lax Level Jumps).
- Lógica asimétrica Lax: Para relaciones `hijo`, el nivel candidato debe ser estrictamente MAYOR al actual (`> cLevel`). Para `padre`, debe ser MENOR (`< cLevel`).
- Pago de deuda técnica detectada en S8.5: Implementación de suite TDD en `Subgrid_Lifecycle.test.js` dedicada a verificar el `rulesContext` completo (incluyendo Orphan Stealing).
- Inyección en UI verificada (ya cubierta pasivamente por la arqutectura S8.5.1 Isomorphic).

**Out of Scope:**
- Detección de Colisiones entre hermanos (`siblingCollisionCheck`) — Queda para otro sprint.
- Rutinas de ciclo preventivas profundas (`preventCycles` más allá de jerarquía matemática).

## Done Criteria
- `SubgridState` procesa `strictLevelJumps: false` descartando opciones matemáticamente incompatibles (inviables jerárquicamente).
- 100% Passing Tests en Jest validando los flujos Strict, Lax y Orphan.
- Merge a la rama develop efectuado con limpieza visual intacta.
