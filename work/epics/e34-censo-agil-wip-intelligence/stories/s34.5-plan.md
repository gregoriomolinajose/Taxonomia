# Story Plan: S34.5 - Mapa de Capacidad E2E

## Task List

- [ ] **S34.5.1: Enrutamiento y Navegación Base (`UI_Router.client.js` e `Index.html`)**
    - [ ] Agregar `<template id="tmpl-capacity-map">` y la inyección `<?!= include('UI_View_CapacityMap'); ?>` en `Index.html`.
    - [ ] Crear el archivo base en `src/UI_View_CapacityMap.html`.
    - [ ] Registrar la pestaña del Menú `"nav-item-capacitymap"` en `UI_Router.renderMainNav`.
    - [ ] Extender `UI_Router.navigateTo(viewType, entityKey, payload)` para soportar un posible objeto config (para el Drill-down).
- [ ] **S34.5.2: Motor DataView Drill-Down (`DataView_UI.client.js`)**
    - [ ] Modificar el método público `render(entityName, containerId, config)`.
    - [ ] Leer `config.filterQuery` y aplicar al estado `_state.filtered`, o bien rellenar el input `<ion-searchbar id="dv-search-input">` y forzar invocación a `_onSearch()`.
- [ ] **S34.5.3: Motor de Renderizado Top-Down (`UI_View_CapacityMap.html`)**
    - [ ] Nivel 1: Portafolio (Header/Section Principal). Calcular y mostrar suma total de Personas.
    - [ ] Nivel 2: Grupos de Producto. Render en Columnas o Secciones Flex. Calcular y mostrar suma total de Personas.
    - [ ] Nivel 3: Equipos. Tarjetas (Cards) hijas del Grupo. Calcular y mostrar suma total de Personas en el Badge.
    - [ ] Eventos onClick de Cards -> `UI_Router.navigateTo('dataview', 'Target_Entity', { filterQuery: 'ID_Padre' })`.
- [ ] **S34.5.4: Suite E2E de Playwright**
    - [ ] Evaluar que el mapa renderice correctamente bajo el jerarquico Port > Grupo > Equipo.
    - [ ] Validar que el DataStore suma consistentemente las Personas sin queries al Backend.
