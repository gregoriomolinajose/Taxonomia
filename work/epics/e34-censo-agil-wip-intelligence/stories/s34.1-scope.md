# Scope S34.1: Schema Relación Persona↔Equipo

## In Scope
- Modifying `Schema_Engine.gs` file only (and `Controller_Lookups.gs` if needed for typo).
- Restoring Jest tests in `Equipo_CRUD_Fix.test.js` to ensure the schema rules apply.
- Adding relation fields for tracking `Persona` and `Equipo` temporally using `PERSONA_EQUIPO` edge type.
- Fixing relation field properties for `Producto` inside `Equipo` (parent direction vs child direction).

## Out of Scope
- Actually building the dynamic backend endpoints or triggers to count these users (This belongs to S34.2).
- Building the UI logic to render the fields optimally (This belongs to S34.3).
- Data migration scripts for pre-existing flat fields (If needed, handled on demand).

## Done criteria
- Schema Engine defines all relationship attributes faithfully according to Graph Architecture without warnings.
- Test Suite runs in green including the previously skipped Equipo tests.
