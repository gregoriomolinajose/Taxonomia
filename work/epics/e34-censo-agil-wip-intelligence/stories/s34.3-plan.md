- [ ] **S34.3.1: Actualizar `Schema_Engine.gs` (Topología 1:1 vs M:N)**
    - [ ] Reestructurar `scrum_master_id` a tipo `relation` (Sub-tipo Single)
    - [ ] Reestructurar `product_owner_id` a tipo `relation` (Sub-tipo Single)
    - [ ] Agregar campo `rte_id` de tipo `relation` (Sub-tipo Single)
    - [ ] Añadir atributo `allowedRoles: ['Developer', 'Tester', 'Tech Lead']` al subgrid `personas_asignadas`.

- [ ] **S34.3.2: Render UI Reactivo `UI_SubgridBuilder.client.js`**
    - [ ] Modificar `_refreshList()` para inyectar `${rol_agil} •` antes del estado.
    - [ ] En la generación de `availableOptions`, inyectar lógica de filtrado contra `field.allowedRoles` si existe.

- [ ] **S34.3.3: Refactorización I/O Backend `Engine_DB.js`**
    - [ ] Establecer `let cachedGraphFull = null;` en `orchestrateNestedSave`.
    - [ ] Sustituir la petición ciega de `fullGraph` en el Pre-hook Topológico por la persistencia en Memoria (Cache JIT).
    - [ ] Sustituir la petición en el Post-hook Materializado de la S34.2 (Agregación) por lectura a `cachedGraphFull`.
