# S44.10: Migración Masiva de Roles - Implementation Plan

## Task Breakdown

### Phase 1: Engine Integrator
- [x] Crear macro script `src/Jobs_Migracion_Roles.js`.
- [x] Desarrollar la función Wrapper `JOB_MigrarRolesAGrafos()`.
- [x] Proteger la carga garantizando que se cuenta con el módulo `Engine_DB` inyectado.

### Phase 2: Interceptor Pattern
- [x] Bypassear a mano los límites que el `Schema_Engine` le impone a `Adapter_Sheets`, consumiendo `getDataRange().getValues()` crudo de `DB_Persona` para rescatar la columna `rol_agil` (eliminada reciéntemente del Frontend Schema).
- [x] Cargar catálogo O(1) de Entidades Rol a un Diccionario In-Memory JS.
- [x] Configurar *Case Insensitive Matching* y Alias en duro ("PO", "Product Owner", "Developer").

### Phase 3: Graph Committer
- [x] Implementar estructura ciclada `edgeToCreateMap` mapeando `[id_persona]` hacia `Array<rolesUUIDs>` por si en la celda habían separado posiciones con coma.
- [x] Redirigir el Write a `Engine_Graph.patchSCD2Edges` garantizando que todo cambio de histórico preserve la sanidad Temporal Slowly Changing Dimension.
- [x] Configurar flush individual (`.setValue("")`) sobre la celda nativa después de éxito total.

### Phase 4: Delivery
- [x] Integrar `Jobs_Migracion_Roles.js` al stack virtual HTML dentro del pipeline de `.build/`. (No aplica a `Client.js` por tanto se asimila directamente por Clasp / Backend parser de Apps Script).
- [ ] Ejecutar prueba real de migración en la Infraestructura Live en nube (Responsabilidad de Humano/Admin).
- [ ] Documentar el Cierre en Retrospectiva y `s44.10-review.md`.
