# S44.3: Mutación DataStore (_nombre_completo)

## 1. Meta Context
- **Epic**: E44 (Organizational Topology)
- **Status**: Completed
- **Primary Objective**: Fusionar los campos `nombre` y `apellidos` de la entidad `Persona` desde la Ingesta OnLoad para unificar su presentación en todas las vistas visuales (`UI_DataGrid`).

## 2. Boundaries & Context
- **In Scope**:
  - Modificación del DataStore local para soportar campos calculados y coalescencia de `_nombre_completo`.
  - Extensión de metadatos `uiConfig` para el soporte gráfico UI en el Grid (avatar, correo y departamento).
- **Out of Scope**:
  - Lógica relacional bidireccional hacia el Grafo Topológico.
