# S44.13: Implementation Plan

- `[ ]` **Setup & Schema:** Configurar y aislar el entorno en las entidades objetivo.
- `[ ]` **Core Logic:** Construir interceptores, caché y lógica de negocio pura.
- `[ ]` **User Interface:** Vincular el puente declarativo hacia el Renderizador HTML.
- `[ ]` **E2E & Validation:** Asegurar flujos sin cuellos de botella (Timeouts) y comportamiento exacto.

## Design

### Architecture & Data Model
- **Data Model**: Extend `hydrateAndDeduplicate` to explicitly act as the Graph Memory Array builder.
- Bypassing the nested object model instantiation, caching directly constructed arrays representing `Sys_Graph_Edges` relations.

### Business Logic & Flow
- **Memory Caching**: Array storage of `PERSONA_CARGO` nodes built structurally in O(1) runtime.
- Use atomic Bulk arrays instead of individual recursive iteration locks (`Engine_DB.orchestrateNestedSave`).
- Inject directly to the destination via a single nuclear `_Adapter_Sheets.upsertBatch` dump per mapping instance, shielding the Google App Script 6 minute limitation.

### UI & UX Integration
- Allow infinite scale capacity inside the Bulk ETL importer. No front-end logic changes require except loading bar optimization polling.

## Implementation Plan

- `[ ]` **Setup & Schema:** Allocate array cache scope variables directly to `Engine_ETL`.
- `[ ]` **Core Logic:** Write transactional bypass transferring relation construction onto the memory block array.
- `[ ]` **User Interface:** Enable the Bulk Matrix mapping switch inside the Frontend Modal ETL endpoint.
- `[ ]` **E2E & Validation:** Stress test a mock set of 1,500 personas, checking total edge creation respects under 30 seconds timing.
