# Scope S44.2: Extensión del Grafo Capacidad

## In Scope
- Añadir campo `peso_capacidad` (Number) a `APP_SCHEMAS.Sys_Graph_Edges`.
- Añadir campo `metadata` general JSON opcional a `Sys_Graph_Edges`.
- Ajustar (Swap invertido) `relationType` entre `Persona` (`padre`) y `Rol` (`hijo`).

## Out of Scope
- Interfaz gráfica para alterar dichos campos en la UI de Formularios normales. (Manejado por vistas específicas más tarde o vía subgrid).
- Lógica sumatoria visual de capacity.

## Done Criteria
- [ ] Schema_Engine.js se carga sin errores.
- [ ] La relación M:N respeta la arquitectura de "Catálogo-Instancia".
