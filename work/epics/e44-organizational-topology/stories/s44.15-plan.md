# S44.15: Implementation Plan

## Milestone 1: Corrección Lógica de Extracción ETL y UUID
- [x] **Tarea 1.1:** Modificar `src/Engine_ETL.js` para sustituir la consulta unitaria `Engine_DB.read('Cargo')` por una consulta colectiva `Engine_DB.list('Cargo', 'objects')`. Esto garantiza la población íntegra del diccionario de resolución `cargoExternoMap` previo a la fase de ingesta (JIT Deduping).
- [x] **Tarea 1.2:** Ajustar en `Engine_ETL.js` la instrucción de autogeneración de Cargo (`GeneradorId.crear`) para invocar explícitamente el método `.toUpperCase()`, previniendo llaves lexicográficas inválidas en el DataStore.

## Milestone 2: Restauración de Vistas y UI/UX
- [x] **Tarea 2.1:** Restaurar físicamente en el repositorio los contenedores base CSS `src/CSS_App.html` y `src/CSS_ETL_Modal.html` y confirmar su inclusión dentro de las directivas del pre-compilador (deploy.js) para evitar pantallazos blancos en Producción.
- [x] **Tarea 2.2:** Actualizar `src/Schema_Engine.js` cambiando el metadata del catálogo Cargo: sustituir el icon_name de `briefcase-outline` a `id-card-outline` y asignar el semantic-token `tertiary` como color base.

## Milestone 3: Implementación de Silent JIT Rehydration (Sync UI)
- [x] **Tarea 3.1:** Localizar el bloque condicional resolutivo en `src/Schema_Studio_UI.html` -> `triggerWsSync()`.
- [x] **Tarea 3.2:** Reemplazar el viejo mecanismo destructor (`DataStore.invalidate('Persona')`) que obligaba al refresh manual, por un bloque de lógica reactiva:
  - Mostrar feedback local visual: "Refrescando memoria local...".
  - Disparar `Promise.all` con `DataAPI.call('getInitialPayload')` simultáneamente para Persona y Cargo.
  - Parsear el JSON crudo en caso de venir stringificado e inflar de nuevo los diccionarios usando `window.DataStore.set()`.
  - Disparar `window.AppEventBus.publish('DATA::UPDATED')` para notificar al DataGrid subyacente o otras interfaces dependientes.
  
## Milestone 4: QA y Release
- [x] **Tarea 4.1:** Ejecutar comandos nativos de despliegue `npm run deploy:dev:auto` y `npm run deploy:prod:auto` en configuración zero-touch para inyectar la actualización final v1.2.8.
- [x] **Tarea 4.2:** Confirmar por parte del usuario final la usabilidad general, superación del problema de caché y correcta pre-visualización de iconografía en la versión en vivo.
