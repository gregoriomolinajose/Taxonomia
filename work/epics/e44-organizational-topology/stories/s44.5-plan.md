# Plan: S44.5 Alertas Capacity Map

Este plan desglosa la implementación práctica de la S44.5, modificando fundamentalmente `UI_View_CapacityMap.html`.

## Step 1: Pre-cómputos Temporales O(N)
- [ ] Ubicar el iterador principal del grafo (`edges.forEach(...)` sobre `Sys_Graph_Edges`) dentro de la función `render` en `UI_View_CapacityMap.html`.
- [ ] Declarar `_personaGlobalCapacity` (`Map()`) para acumular peso cruzado por cada `id_nodo_hijo` que representa una Persona, donde la relación sea estricta (`PERSONA_EQUIPO`) y vigente (`es_version_actual !== false`).
- [ ] Declarar en el mismo bloque iterativo banderas de orfandad (e.g. `_teamHasLeadership` `Map()`) si detectamos aristas con tipos `EQUIPO_SM`, `EQUIPO_PO` que apunten al equipo en cuestión como padre o nodo_central.

## Step 2: Detección In-Situ sobre Nodos Híbridos
- [ ] Tras el cálculo crudo (cuando pasamos a agrupar los pesos por equipo dentro de `getPersonsByTeam` o en la iteración principal), flaggear qué equipos tienen específicamente en sus filas a Personas cuyo `_personaGlobalCapacity[id]` sea estrictamente `> 100`.
- [ ] Inyectar metadatos booleans `hasOverloadedMembers` y `isOrphanLeadership` en las métricas de capacidad por equipo devueltas o almacenadas.

## Step 3: Manipulación del DOM (Pintado Visual)
- [ ] Modificar la firma de entrada en la factoría visual `createClickableCard(title, subtitle, metrics, navData, colorClass)` agregando el objeto completo de anomalías topológicas u orquestándolo desde el parámetro `metrics`.
- [ ] En la concatenación visual del Nodo/Título, inyectar dinámicamente insignias ROJAS de Iconos de precaución de Ionic (`<ion-icon name="warning-outline" color="danger"></ion-icon>`) si se activan las fallas de Orfandad (>100% / No Leadership).
- [ ] Ajustar estética (Tooltips CSS Nativos / Tokens semánticos) si hubiera colapso espacial.

## Step 4: Quality & Integrity
- [ ] Confirmar que el layout no sufre degradación (Overflow en el Header Card del DOM). No destruir la UI para celulares.
- [ ] Confirmar compatibilidad y Graceful Error si no existiese ningún Grafo Activo.
