## Architecture Review: s44.15 (scope: story)

### Critical (fix before merge)
*Ninguno.*

### Recommended (simplify before next cycle)
- **`src/Schema_Studio_UI.html` : L1965** - **H10 (Pattern Duplication)**.
  **Observación:** Se ha introducido lógica de descompresión de tuplas (`headers` / `rows.map`) directamente dentro del bloque `.then` del `Promise.all` para re-hidratar `Persona` y `Cargo`. Esta lógica es un duplicado exacto del descompresor que ya existe en `DataView_UI.client.js` (L176) y en otros lugares del cliente.
  **Recomendación:** Considerar mover esta lógica de "inflado de tuplas a objetos" hacia una función de utilidad global (ej. `window.Schema_Utils.inflateTuples(response.data)`) o abstraer la llamada completa de sincronización silenciosa hacia `JS_Core.client.js` en una próxima iteración, para evitar que la UI del Schema Studio asuma la responsabilidad de parsear el protocolo de red.

### Questions (require human judgment)
- **`src/Engine_ETL.js` : L186** - **H8 (Configuration Over Convention)**.
  Al cambiar de `.read` a `.list('Cargo', 'objects')`, ahora se trae la colección entera a la RAM del servidor GAS durante la ejecución masiva. ¿Tenemos certeza de que el volumen máximo esperado de "Cargos" nunca excederá el límite de memoria del entorno de Apps Script (aprox. 50MB por invocación) durante una recarga masiva de Workspace?

### Observations (patterns noted)
- **H2 (Necessity / Idempotencia)**: La adición de `.toUpperCase()` en la generación de llaves para Cargos es proporcional y sumamente necesaria para mantener la invariancia de llaves maestras en el motor relacional.
- **H9 (Semantic Duplication)**: Restaurar `CSS_App.html` y `CSS_ETL_Modal.html` rompe la directiva Zero-Touch de empaquetado solo si estos CSS ya estaban incrustados en los componentes nativos. Dado que se resolvió un bug de despliegue, la acción fue justificada, pero vigilar el pipeline del bundler en el `deploy.js` para evitar colisiones futuras.

### Verdict
- [x] PASS WITH QUESTIONS
