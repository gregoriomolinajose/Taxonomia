# S44.8: Implementation Plan

- `[ ]` **Setup & Schema:** Configurar y aislar el entorno en las entidades objetivo.
- `[ ]` **Core Logic:** Construir interceptores, caché y lógica de negocio pura.
- `[ ]` **User Interface:** Vincular el puente declarativo hacia el Renderizador HTML.
- `[ ]` **E2E & Validation:** Asegurar flujos sin cuellos de botella (Timeouts) y comportamiento exacto.

## Design

### Architecture & Data Model
- **Dictionary Context**: Extrapolate `Cargo` as an independent dictionary node, mapping Workspace's text IDs to internal UUIDs.
- **Topological constraint**: Link `Cargo` as a Catálogo node, enforcing `isTemporalGraph` rules for 1:N relations.

### Business Logic & Flow
- Cache dictionary resolutions internally so ID searches don't consume repeated I/O queries.
- Build interceptor to automatically construct a `CARG-[...] ` UUID natively assigned by the DataStore.

### UI & UX Integration
- Embed standard CRUD interfaces to map strings from "ID Externo Workspace" seamlessly for HR administrators without complex technical configuration screens.

## Implementation Plan

- `[x]` **Setup & Schema:** Instantiate `APP_SCHEMAS.Cargo` inside the Core dictionary.
- `[x]` **Core Logic:** Ensure relationships to `Persona` respect SCD2 historical timelines.
- `[x]` **User Interface:** Enable the "Cargo" entity on the Data API and left navigation sidebar.
- `[x]` **E2E & Validation:** Complete Unit tests evaluating historical promotion path creation.
