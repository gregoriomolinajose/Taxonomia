# S44.11: Sincronización On-Demand (Workspace Sync)

## User Story

**As a** System Administrator
**I want** an on-demand trigger button in the System Admin view
**so that** any Persona record in the database missing Workspace information (e.g. `nombre`, `id_cargo`, `departamento`) is automatically queried and updated based on its `email`, without requiring a complex time-driven architectural footprint.

## Acceptance Criteria

```gherkin
Feature: Workspace On-Demand Synchronization

  Scenario: Execution of Manual Sync Job
    Given there are Persona records where the `nombre` or `id_cargo` is empty but the `email` is present
    When the Admin clicks the "Sincronizar" button in the System profile view
    Then the engine iterates through the missing records in secure batches
    And invokes `resolverDirectorioWorkspace` for each `email`
    And natively patches the Persona using the `Engine_DB.update` layer
    And routes the `cargo` field through the Auto-Discovery mapping interceptor.
```

## Specification By Example

- Cuando el administrador lanza el proceso desde una hipotética Consola de QA (o se corre por defecto a las 2 AM):
- El script lee solo las Personas que están incompletas.
- Pasa la Data por el mismo flujo `Engine_ETL.hydrateAndDeduplicate()` o inyecta la solución directamente en un Batch Update.


### Architecture & Data Model
- Asegurar estricta alineación con los requerimientos topológicos de `S44.11: Sincronización Programada y On-Demand (Workspace Sync)`.
- Definir la representación estructural y restricciones (Constraints) en `APP_SCHEMAS` o Graph Engine.

### Business Logic & Flow
- Resolver propagación atómica de estado en `Adapter_Sheets.upsertBatch`.
- Añadir Hard Fallback for `workspace_sync_status`.
- Evaluar seguridad (ABAC) y propagar las aristas SCD-2 si corresponden.

### UI & UX Integration
- Conectar componentes dinámicos como FormBuilders, TXSearchables o DataGrids.
- Respetar los patrones de la capa de presentación S44 (High Density, Skeleton Loading).
