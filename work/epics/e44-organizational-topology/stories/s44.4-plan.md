# Implementation Plan: S44.4

## Phase 1: Engine_ABAC Hardening & FLS
1. Editar `src/Engine_ABAC.js` (siendo el validador principal que corre en GAS).
2. Adicionar el método `stripFieldPayload(email, entityName, payload)` que analice el Nivel de Acceso del emisor.
3. Si el schema a grabar es `Persona` y contiene la propiedad `id_rol` adjunta, determinar si el Nivel es `"ALL"`. Si no lo es, borrar (`delete payload.id_rol;`) e inyectar un log al `Logger.log` del lado del servidor advirtiendo el intento de escalación.

## Phase 2: Interceptor Orchestration
1. En `src/Router.js`, ubicar la función `apiGrabarRegistro()`.
2. Justo antes de ejecutar `Engine_DB.grabar()`, invocar `payload = Engine_ABAC.stripFieldPayload(Session.getActiveUser().getEmail(), entityName, payload)`.

## Phase 3: Candado Workspace Config
1. Modificar la función `validatePermission(email, action, entityName)` en `Engine_ABAC.js`.
2. Agregar un clausulado de excepción estricta para `Config_Workspace` al principio de la matriz bypass de degradación. Si `entityName === 'Config_Workspace'`, retornar incondicionalmente `false` a menos que exista una regla física matricial asignada que indique permisos absolutos para ese rol.
