# Implementation Plan: S44.3

## Phase 1: Declarative Schema Layout
1. Mutar la configuración de la entidad `Persona` en `Schema_Engine.js`.
2. Asignar genérico `uiConfig.dashboardCard: { avatarField: 'avatar' }`.
3. Asignar `subtitleFields` apuntando a `email` (con su ícono `mail-outline`) y `departamento` (`business-outline`).
4. Definir array universal de `computedFields: { name: '_nombre_completo', concat: ['nombre', 'apellidos'], separator: ' ', fallback: ['email', 'id_persona'] }` para Persona.

## Phase 2: Engine Aggregation (JS_Core)
1. Refactorizar el motor de hidratación base `DataStore.set()` en `JS_Core.client.js`.
2. Incluir el iterador genérico `schema.computedFields` para generar propiedades de VDOM.
3. Evaluar arrays Nulos o Vacíos (`if(data[j][concat[k]])`) para no contaminar valores de fallback como "undefined undefined".

## Phase 3: Visual Grid Hooking (DataGrid)
1. Ajustar `UI_DataGrid.client.js` en su método `_renderGridView` para consumir pasivamente `uiConfig`.
2. Reemplazar la directiva estática SVG de `ion-icon` por una etiqueta `<img>` fluida y de Avatar si se detecta firma web (`startsWith('http')`) en el correspondiente `avatarField`.
3. Iterar genéricamente `dCard.subtitleFields` para imprimir las rows secundarias en el DOM de Render de las Modern Cards.
