# Scope: S50.1 Schema Updates (Foundation)

## Boundaries

**In Scope:**
- Agregar `especialidad` a la definición de `Rol` en `Schema_Engine.js`.
- Agregar `contexto_id` a la definición de `Sys_Graph_Edges` en `Schema_Engine.js`.

**Out of Scope:**
- Modificar componentes de UI o formularios para usar estos nuevos campos. (S50.2 y S50.3).
- Ejecutar migraciones SQL o lógicas ETL.

## Done Criteria
- `Schema_Engine.js` contiene los dos nuevos campos en sus respectivas entidades.
- No hay errores de sintaxis en el esquema central.
