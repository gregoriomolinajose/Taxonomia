# Scope: S50.3 UI Hydration Context Flag

## In Scope
- Modificar la construcción de componentes en `UI_SubgridBuilder.client.js`.
- Modificar el renderizado y obtención de datos en `UI_Component_TXSearchable.client.js` para recibir y propagar `contextId`.
- Habilitar que el subgrid consulte a `Graph_Utils` usando el bypass implementado en la S50.2.

## Out of Scope
- Aprobación masiva de borradores (eso pertenece a la S50.4).
- Modificar cómo se renderizan otros módulos fuera del Modal / Wizard.
- Modificar lógica de backend. Todo es front-end (UI).

## Done Criteria
- `TXSearchable` y las grillas dependientes del `SubgridBuilder` pueden mostrar relaciones de borrador activas.
- No hay derrames de datos (borradores de "TAX-2" no se muestran en el wizard de "TAX-1").
- El código sigue respetando Beck's Simple Design rules (KISS/YAGNI).
