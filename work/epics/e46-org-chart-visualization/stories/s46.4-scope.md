# S46.4 Scope

## In Scope
- Modify `DataEngine_UI.client.js` `buildHierarchyTree` to use `lider_directo` as `parentCol`.
- Ensure UUIDs are correctly extracted from relational graph objects.
- Maintain synthetic fallback roots (e.g., "Empresa").

## Out of Scope
- Changing the visual layout or vendor library (ApexTree).
- Edits or drag-and-drop of the hierarchy structure.

## Done Criteria
- `buildHierarchyTree` safely parses `lider_directo`.
- Dev build generates the tree matching the graph topology correctly.
