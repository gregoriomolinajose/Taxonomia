# Implementation Plan: S46.5

## Phase 1: Base Integration
- [ ] 1. **DataView Toolbar:** Modificar `src/UI_DataView_Toolbar.client.js` inyectando un botón (ej. ícono `analytics-outline`) para Capacidades, que active `onViewToggle('echarts')`.
- [ ] 2. **DataView Engine:** Modificar `src/DataView_UI.client.js` en la función `_rerenderData()` para soportar el caso `_state.view === 'echarts'` invocando a `UI_View_ECharts.render(dataZone, _state)`.

## Phase 2: Component Layout & Data Mapping
- [ ] 3. **Nuevo Motor Visual:** Crear el archivo `src/UI_View_ECharts.client.js` con el patrón IIFE (global module) manejando el montaje y el ResizeObserver para el `chartContainer`.
- [ ] 4. **Math Engine Query:** Crear función helper `window.Math_Engine.buildEChartsCapacidadesOption(data)` (en `Math_Engine.html` u otro engine) que procese las filas en el array/objeto compatible con un diagrama ECharts (por defecto utilizaremos un gráfico de nodos (Tree o Graph) para visualizar topología).

## Phase 3: Deployment & Validation
- [ ] 5. Registrar el nuevo archivo `UI_View_ECharts.client.js` si hay que agregarlo a rutinas especiales (si el bundle automático no lo toma, aunque parece tomar todos los `*.client.js`).
- [ ] 6. Despliegue en entorno `dev` mediante comando `npm run deploy:dev:auto`.
- [ ] 7. Verificar comportamiento de carga cruzada e instanciación de ECharts.
