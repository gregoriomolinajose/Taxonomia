# Scope S46.3: Node Template & Theme Styling

## In Scope
- Refactor the `nodeTemplate` in `DataView_UI.client.js` to extract it into a more manageable structure (or keep it inline if simple, but it must be rich).
- Include CSS styles for the `.org-node` class.
- Add Avatar rendering (using fallback initials if no Avatar is present).
- Implement a color hash function or deterministic color mapping based on `content['Área']` (or `Departamento`).
- Ensure all interpolated strings in the template are passed through `escapeHTML`.

## Out of Scope
- Interactive CRUD inside the tree (e.g., drag and drop to change leader).
- Export to PDF.

## Done Criteria
- [ ] Nodes display Avatar, Name, Role, Department.
- [ ] Color coding is visible and distinct per department.
