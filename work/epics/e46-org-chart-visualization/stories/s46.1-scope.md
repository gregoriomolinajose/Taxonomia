# Scope S46.1: Toolbar View Toggle & Data Transformer

## In Scope
- Add HTML/CSS for the new Tree View toggle button in the `UI_DataView` toolbar.
- Update `UI_DataView` javascript state to manage `currentView = 'tree'`.
- Implement `buildHierarchyTree(records)` function in a new or existing data utility file to build parent-child relations using `Líder`.

## Out of Scope
- Actually rendering the ApexTree diagram (that's S46.2).
- Node templating and styling (that's S46.3).

## Done Criteria
- [ ] Button exists and toggles state correctly in the UI.
- [ ] List/Grid views hide when Tree view is active, displaying a placeholder container.
- [ ] `buildHierarchyTree` is written and verified to output the correct JSON shape.
