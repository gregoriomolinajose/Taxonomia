# Scope: S19.1 (Truthiness Refactoring)

## Boundaries

### In Scope
- Refactorización estricta del validador de `FormValidators.html`.
- Cambios a nivel semántico de coercion asimétrica (`|| 0` a chequeo por `isNaN`) en `Dashboard_UI.html`, `SubgridState.js` y `Controller_Lookups.gs`.

### Out of Scope
- Intervenciones directas sobre APIs del CMS subyacente.

## Done Criteria
- [ ] Chequeos de validación en HTML no emplean `if (!value)` para control de strings y nulos, usando operadores estructurales.
