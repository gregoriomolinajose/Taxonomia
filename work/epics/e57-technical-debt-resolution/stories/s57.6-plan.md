# Plan: S57.6 Optimización de Reactividad y Latencia de Relaciones en el Lienzo

## Fase 1: Ruteo de Eventos e Identificación de Interacciones de Usuario
1. **[ ] Task 1:** En `UI_Component_TXSearchable.client.js`, añadir parámetro `isUserEvent` (default `false`) a la firma y detalle de evento de `dispatchSelection`.
2. **[ ] Task 2:** En `UI_Component_TXSearchable.client.js`, actualizar las invocaciones de `dispatchSelection` en escuchadores de clicks/teclado para enviar `true`.
3. **[ ] Task 3:** En `UI_Factory_Searchables.client.js`, capturar `isUserEvent` del evento `txChange` e inyectarlo en el detalle del evento relanzado `ionChange`.

## Fase 2: Evitar Bloqueo de OptimisticLock en Hidratación
4. **[ ] Task 4:** En `UI_Component_RelationBuilder.client.js`, condicionar el `optimisticLock` y alertas de cambio de padre a que `ev.detail && ev.detail.isUserEvent` sea estrictamente verdadero.

## Fase 3: Optimización del Resolutor de Catálogos (Lookups)
5. **[ ] Task 5:** En `FormEngine_Resolvers.client.js`, comprobar en `hydrateLookupSources` si `apiMethod === 'getInitialPayload'` y `window.DataStore.get(apiArgs[0])` contiene registros. Si es así, resolver y despachar inmediatamente de forma local (0ms).

## Fase 4: Validación y Despliegue
6. **[ ] Task 6:** Verificar la carga y funcionalidad sin bloqueos en el lienzo localmente.
7. **[ ] Task 7:** Ejecutar `npm run deploy:dev:auto` para desplegar a desarrollo.
