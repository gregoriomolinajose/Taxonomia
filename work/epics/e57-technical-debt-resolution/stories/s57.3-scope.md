# Scope: S57.3 Habilitar TXSearchable para Relaciones Jerárquicas

## In Scope
- Habilitar el componente `searchable_single` (TXSearchable) para el campo `relaciones_padre` de la entidad Dominio.
- Habilitar el componente `searchable_single` (TXSearchable) para el campo `id_dominio_padre` de la entidad Capacidad.
- Habilitar el componente `searchable_multi` (TXSearchable) para los campos de subordinados en ambas entidades (este comportamiento ya era el nativo pero se confirma formalmente en la historia).
- Ajustar el color del icono de Dominio de `secondary` a `primary` para mayor visibilidad.
- Desplegar cambios a los entornos `dev` y `prod`.

## Out of Scope
- Refactorizar el motor de renderizado de `UI_Component_TXSearchable`.
- Alterar las validaciones topológicas de ambas entidades.

## Done Criteria
- `Schema_Engine.js` contiene la definición `uiComponent: "searchable_single"` para los padres de ambas entidades.
- `Schema_Engine.js` contiene `color: "primary"` para el Dominio.
- El CRUD interactivo lateral carga los campos jerárquicos usando el componente `tx-searchable` con la capacidad de lanzar el modal de selección.
- Despliegues validados.
