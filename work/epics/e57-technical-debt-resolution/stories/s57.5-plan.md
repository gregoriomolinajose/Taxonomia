# Plan: S57.5 Cálculo Automático de Niveles Jerárquicos

## Fase 1: Ajuste de Esquemas y Eliminación de Triggers
1. **[x] Task 1:** En `Schema_Engine.js`, marcar el campo `nivel_tipo` como `readonly: true` en las entidades `Dominio` y `Capacidad`.
2. **[x] Task 2:** En `Schema_Engine.js`, eliminar la matriz `triggers` previamente agregada para `nivel_tipo`.

## Fase 2: Desbloqueo Permanente de Relaciones
3. **[x] Task 3:** Actualizar `SubgridState.client.js` o `TOPOLOGY_PRESETS` en el esquema para que la selección del "Padre" no requiera un nivel pre-existente, habilitando el campo permanentemente durante la edición.
4. **[x] Task 4:** En `UI_Component_RelationBuilder.client.js`, añadir lógica para ocultar el nodo contenedor del padre si el formulario está `isActuallyReadonly` y el dataset de seleccionados está vacío (Ocultar Nodos Raíz en modo consulta).

## Fase 3: Auto-cálculo en Persistencia (Math_Engine)
5. **[x] Task 5:** En `Math_Engine.html`, crear la función pura `calculateHierarchyLevel(formStateObj, params, cache)` que resuelva la profundidad del nodo iterando sobre la topología (o sumando 1 al nivel del padre). Exportar la función.
6. **[x] Task 6:** Interceptar el guardado en `UI_FormSubmitter.client.js` e invocar a `Math_Engine.calculateHierarchyLevel` antes de serializar el payload, inyectando el valor calculado en el campo `nivel_tipo`.

## Fase 4: Pruebas y Despliegue
7. **[x] Task 7:** Validar end-to-end la creación de un Dominio Hijo, verificando que se guarde con el nivel correcto.
8. **[x] Task 8:** Ejecutar `npm run deploy:dev:auto` y validar.
