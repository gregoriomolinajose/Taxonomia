# Plan: S57.3 Habilitar TXSearchable para Relaciones Jerárquicas

## Fase 1: Actualización de Metadatos y Schema
1. **[X] Task 1:** Modificar la definición de `Dominio` en `Schema_Engine.js`.
   - Cambiar `uiComponent` de `select_single` a `searchable_single` en el campo `relaciones_padre`.
   - Cambiar la propiedad `color` en la metadata de `secondary` a `primary` para solventar problemas de legibilidad visual.
2. **[X] Task 2:** Modificar la definición de `Capacidad` en `Schema_Engine.js`.
   - Cambiar `uiComponent` de `select_single` a `searchable_single` en el campo `id_dominio_padre`.

## Fase 2: Despliegue y Validación
3. **[X] Task 3:** Ejecutar `npm run deploy:dev:auto` para traspilar y desplegar los cambios del esquema al entorno de Desarrollo.
4. **[X] Task 4:** Ejecutar `npm run deploy:prod:auto` para promover los cambios a Producción.
5. **[X] Task 5:** Validar desde el front-end que el formulario del CRUD levante correctamente el elemento Web Component `<tx-searchable>`.
