# Scope: S3.2 Auto-Pathing Visual & Hidratación UI

## In Scope
- Addition of `calculatedValue` to `path_completo_es` in `Schema_Engine.gs`.
- Addition of `triggers_refresh_of` to the name input (`n0_es`).
- Refactoring `openEditForm` in `FormEngine_UI.html` to execute local `lookupSource` functions during record hydration to populate `<ion-select-option>` items in RAM.
- Creation of `getCalculatedPathEs` client-side resolver.

## Out of Scope
- Backend modifications or remote resolution for hierarchy trees.
- Refactoring of how relations persist in the DB (only visual and pre-population mechanisms).

## Done When
- [ ] Schema_Engine.gs is updated.
- [ ] FormEngine_UI.html pre-hydrates `<ion-select>` options for locked relationship fields.
- [ ] The full path string calculates interactively.
- [ ] Code passes test verifications and is deployed to dev.
