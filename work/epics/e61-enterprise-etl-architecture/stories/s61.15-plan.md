# S61.15 Plan: Intra-batch Deduplication Bugfix

## Tasks

### Task 1: Update Identity Resolution in Engine_ETL.js
- **File**: `src/Engine_ETL.js` (and optionally `packages/core/src/Engine_ETL.js` if necessary, though src/Engine_ETL.js is confirmed to have the code).
- **Change**: Locate the `if (matchedRow)` block in `hydrateAndDeduplicate`. Update the logic to check `if (!matchedRow[pkField])`.
- **Implementation**:
  - If true, `Object.assign(matchedRow, payload)` and `payload._dropFromBatch = true`.
  - Else, proceed with existing temp ID substitution logic.

### Task 2: Filter Batch in Engine_ETL.js
- **File**: `src/Engine_ETL.js`
- **Change**: At the end of `hydrateAndDeduplicate`, filter out items with `_dropFromBatch = true`.
- **Implementation**: `items = items.filter(p => !p._dropFromBatch);` before returning `{ data: items };`.

### Task 3: Verify and Ensure Quality
- **Review**: Architecture Review and Quality Review.
- **Completion**: Verify the fix passes tests (if applicable) and matches the acceptance criteria.
