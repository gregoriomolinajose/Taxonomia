# Story S61.13 Plan

## Tasks
1. **Engine_DB.js modifications**:
   - Locate `_checkCacheSignals` function in `src/Engine_DB.js` (or wherever it resides).
   - Before `cache.put(signalsCacheKey, JSON.stringify(signals), 60);`, apply `reduce` or similar pruning logic to ensure we only keep the signal with the highest `invalidated_at` for each unique `entity_name` and `by_tenant` combination.
2. **Engine_ETL.js modifications**:
   - Locate the logic assigning `matchedRow` after a DB search in `src/Engine_ETL.js` (or wherever it resides).
   - Change the assignment so that if no row is matched, it explicitly uses `null` instead of implicitly staying `undefined`.
3. **Verification**:
   - Verify code compiles/type-checks.
   - Run tests if applicable.
