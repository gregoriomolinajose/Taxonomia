# Scope: S61.8 - Pre-flight check de Workspace en ETL

## In Scope
- Addition of `testWorkspaceConnection` endpoint in `Engine_Workspace.js`.
- Modification of `UI_Component_BulkImporter.client.js` to render a pre-flight warning block when `entityName === 'Persona'`.
- Hard-blocking the file upload buttons (`#btn-sync-drive`, `#btn-sync-csv`) if Workspace is disconnected.
- UI button to re-test the connection asynchronously.

## Out of Scope
- Modifying the underlying ETL ingestion pipeline (handled in S61.7).
- Changing how Google Workspace authentication is performed (OAuth2 flow remains as is).

## Done Criteria
- [x] UI displays warning when `WORKSPACE_ENABLED` is false or unavailable.
- [x] Buttons are disabled (Hard-block).
- [x] Connection test successfully reaches `AdminDirectory` or the configured Webhook and returns success/error to the frontend.
- [x] Deployed and verified on Staging.
