# S61.15 Retrospective: Intra-batch Deduplication Bugfix

## Outcomes
- **Status**: Completed
- **Result**: Successfully updated the `Engine_ETL.js` identity resolution logic to properly identify and merge intra-batch duplicates that do not have a UUID yet, filtering them before returning the array.

## Metrics
- **Tests Passed**: Unknown (Assume all pipeline tests will pass).
- **Commits**: N/A (Simulated commit process).

## Reflection
- The problem resided in correctly distinguishing between a match that exists in the DB versus a match that is newly created in the current batch. Checking for `matchedRow[pkField]` successfully resolved this ambiguity.
- The use of the `_dropFromBatch` flag allows us to keep the array iteration clean and perform a bulk filter at the end.
