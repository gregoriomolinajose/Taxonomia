# Implementation Plan: S61.8 - Pre-flight check de Workspace en ETL

## Tareas (Resueltas)
1. **[X] Modificar `Engine_Workspace.js`**: Agregar `testWorkspaceConnection` manejando modo Webhook y modo Nativo.
2. **[X] Modificar `UI_Component_BulkImporter.client.js`**:
   - Inyectar contenedor HTML con ID `etl-workspace-preflight`.
   - Modificar `render()` para validar `entityName === 'Persona'` y `window.AppEnv.WORKSPACE_ENABLED`.
   - Aplicar el "Hard-Block" a `#btn-sync-drive` y `#btn-sync-csv`.
   - Añadir listener a `#btn-test-workspace` para llamar a `DataAPI.call('testWorkspaceConnection')`.
3. **[X] Despliegue**: Ejecutar `npm run deploy:staging:auto` (vía `node deploy.js staging --zero-touch`).
