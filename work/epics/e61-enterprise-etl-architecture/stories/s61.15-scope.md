# S61.15 Scope: Intra-batch Deduplication Bugfix

## In Scope
- Fixing the passive deduplication loop in `Engine_ETL.js` to correctly handle intra-batch duplicate resolution.
- Merging information from the duplicate payload to the matched row when both lack a UUID.
- Dropping intra-batch duplicates from the batch before saving.

## Out of Scope
- Modifying cross-batch deduplication logic.
- Changes to database adapter upsert behavior.

## Done When
- [x] Code changes implemented in `Engine_ETL.js`.
- [x] Intra-batch duplicates are filtered out using the `_dropFromBatch` flag.
- [x] Architecture review and quality review pass.
