# Plan: S61.7 - Pipeline de Reglas de Negocio (Workspace Hydration)

## Tareas de Implementación
1. Modificar `src/Business_Interceptors.js`
   - Agregar el interceptor `HydrateWorkspace` en el objeto de `INTERCEPTORS`.
   - Iterar los items, y por cada uno, intentar resolver los datos desde el Workspace (`resolverDirectorioWorkspace`).
   - Sobrescribir los atributos faltantes de los items usando la respuesta de Workspace (como el lider directo, nombre, apellidos, cargo, etc).
2. Modificar `src/Schema_Engine.js`
   - En el esquema de `Persona`, colocar `HydrateWorkspace` de primero en el arreglo `mutationInterceptors`.
3. Verificación
   - Ejecutar pruebas en el ambiente staging con un CSV donde sólo esté la columna de correo de las personas.

## Criterios de Aceptación
- La hidratación de Workspace se ejecuta satisfactoriamente sobre cada item y detona la generación de los enlaces de las entidades en la jerarquía (líder y cargo).
