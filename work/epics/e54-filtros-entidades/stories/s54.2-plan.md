# S54.2: Implementation Plan

## Overview
- **Story:** S54.2 (Lógica Base del Motor de Filtros)
- **Size:** M (Medium)
- **Focus:** Lógica OOP, State Management y renderizado dinámico del DOM.

## Tasks

| ID | Description | Files | Verification | Size | Deps |
|---|---|---|---|---|---|
| T1 | Refactor OOP (Constructor y State) | `src/UI_UniversalFilter.client.js` | Linting JS | S | None |
| T2 | Algoritmos Extractivos (Schemas & Set) | `src/UI_UniversalFilter.client.js` | Linting JS | M | T1 |
| T3 | Renderizado Dinámico del DOM | `src/UI_UniversalFilter.client.js` | Linting JS | M | T2 |
| T4 | Event Listeners & State Mutators | `src/UI_UniversalFilter.client.js` | Linting JS | M | T3 |
| T5 | Manual Integration Test (Sandbox) | `scratch/test_filter.html` | Visual Validation | XS | T4 |

## Execution Order & Rationale
1. **T1:** Modificar la estructura base actual convirtiéndola en una clase `UI_UniversalFilter` para permitir inyección de dependencias (`schemaConfig`, `records`, `onFilterChange`).
2. **T2:** Crear métodos internos (`_getFilterableFields()`, `_getUniqueValuesForField()`) para preparar los datos antes de pintar.
3. **T3:** Remplazar el HTML mockeado en la función `render()` iterando sobre los datos de T2 e inyectando IDs seguros para A11y.
4. **T4:** Enganchar la delegación de eventos (`change`) en los checkboxes para mutar `this.activeFilters` y llamar a `onFilterChange`.
5. **T5:** Probar con datos mockeados complejos en el sandbox.

## Duration Tracking
| Task | Status | Est. Size | Actual Time |
|---|---|---|---|
| T1 | To Do | S | |
| T2 | To Do | M | |
| T3 | To Do | M | |
| T4 | To Do | M | |
| T5 | To Do | XS | |
