# S54.3 Implementation Plan

## Tasks

- [ ] **T1: Inject Filter Toggle Button**
  - Modificar `src/UI_DataView_Toolbar.client.js` (`buildToolbarHTML`).
  - Añadir el parámetro de callback `onFilterToggle`.
  - Reemplazar el `console.log` del botón `#dv-filter-trigger-btn` por la ejecución de `onFilterToggle`.

- [ ] **T2: Setup DataViewEngine State & Container**
  - Modificar `src/DataView_UI.client.js`.
  - En `_state`, inicializar `advancedFilters: {}` y `_filterInstance: null`.
  - En la función `render()`, crear e inyectar un nodo DOM `<div id="dv-filter-container"></div>` dentro del `container` principal o el `root` del dataview.

- [ ] **T3: Initialize UniversalFilter Class**
  - Dentro de `_fetchData` success callback (cuando se tienen los `rows`).
  - Validar si `window.UI_UniversalFilter` está disponible.
  - Instanciarlo en `_state._filterInstance` pasando `entityName`, `APP_SCHEMAS`, `_state.data`, el `container` (recién inyectado), y definir el callback `onFilterChange`.

- [ ] **T4: Implement Filtering Intercept (AND/OR Logic)**
  - Dentro de `_applyFilter(query)` en `DataView_UI.client.js`.
  - Después de aplicar la búsqueda textual libre (o sobre la misma iteración), incluir la lógica de intersección:
    - Iterar las claves de `_state.advancedFilters`.
    - Realizar un AND lógico entre campos.
    - Realizar un OR lógico dentro de los valores de un mismo campo.
  - Asegurar la deduplicación y preservación del grafo (graph hydration JIT) ya existente en `_applyFilter`.

- [ ] **T5: Orchestrate Rerender**
  - En el callback `onFilterChange(newState)` del Drawer, asignar `_state.advancedFilters = newState`.
  - Llamar a `_applyFilter(document.getElementById('dv-search-input').value)`.
  - Re-pintar la UI con `_rerenderData()`.
