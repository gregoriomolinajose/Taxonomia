# Scope: S30.4 - Evolución de ThemeManager

## In Scope
- Refactorizar `src/UI_ThemeManager.client.js` hacia una clase ES6 formal `UI_ThemeManager`.
- Implementar patrón modular exportando la inicialización o manteniendo la exposición de `window.ThemeManager` mediante instanciación controlada (ej. `new UI_ThemeManager()`).
- Conservar los Design Tokens existentes en `ThemeManager.tokens` intactos.
- Trasplantar o refactorizar la lógica de aplanamiento iterativo `_hydrate()` de manera encapsulada.
- Refactorizar los tests correspondientes y/o crear tests en caso de faltar.

## Out of Scope
- Expandir la lista actual de tokens CSS.
- Modificar componentes visuales de Ionic que consumen los tokens.
- Alterar el Pipeline de CSS de Rollup.

## Done When
- `src/UI_ThemeManager.client.js` implemente ES6 Classes strict mode.
- Las variables se rendericen de forma idéntica en el tag HTML raíz (DOM).
- Pruebas unitarias certifiquen que la inicialización y el parseo de default tokens funciona correctamente.
