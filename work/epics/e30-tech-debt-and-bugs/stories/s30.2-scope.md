# Scope: S30.2 Robustez en Muteo Estructural de Relacionales

**In scope:**
- Modificación de `UI_SubgridBuilder` para aceptar `config.readonly` en su instanciación.
- Modificación de `FormRenderer_UI.client.js` para proveer este parámetro en vez de depender de clases CSS restrictivas en el layout contenedor.
- Limpieza de CSS residual proveniente de este hack previo (si existiese).

**Out of scope:**
- Rediseñar el componente `DataGrid` (ya optimizado en E16).
- Mover el motor ABAC (esto pertenece a H2 / S30.x separados).

**Done when:**
- El Subgrid se inicializa dinámicamente y oculta controles interactivos puramente por lógica de render JS.
- Se elimina el ocultamiento global pasivo de punteros.
