## Architecture Review: S67.2 (scope: story)

### Critical (fix before merge)
None.

### Recommended (simplify before next cycle)
- `src/Schema_Engine.js:914` (H9 Semantic Duplication): The list of `backendOnlyFields` is exactly duplicated from `window.CORE_SYS_FIELDS` in `src/Index.html:148`. Simplification: Consider extracting this list to a shared constant or referencing a single source of truth if both run in the Google Apps Script environment, to avoid maintainability issues if new audit fields are added.

### Questions (require human judgment)
- `src/Controller_Action.js:128` (H7): The check for dashboard entities relies on `schema.uiConfig.dashboardCard || schema.uiConfig.dashboardDirectory`. Is this sufficient to catch all entities that need to be pre-loaded, or are there edge cases where data is required immediately but lacks these flags?

### Observations (patterns noted)
- `src/Index.html:101`: Heavy libraries (like apexcharts, echarts, xlsx) correctly deferred using the `defer` attribute to prevent DOM blocking.
- `src/Schema_Engine.js:911`: `getPrunedAppSchema` correctly implemented to strip unnecessary backend metadata from the initial payload.

### Verdict
- [x] PASS WITH QUESTIONS
