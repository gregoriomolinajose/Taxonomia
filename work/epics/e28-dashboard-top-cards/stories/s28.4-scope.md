# Scope: S28.4 - Dashboard Cards Responsivo

## In scope
- Modificación del modelo dimensional `<ion-col>` para flex stretch (`UI_Component_DashboardCardCont.client.js`).
- Refactorización de la inyección DOM de colores priorizando `uiConfig.dashboardCard` (`Dashboard_UI.html`).
- Extensión del bloque `uiConfig` de `Schema_Engine.gs` para admitir el parametro objeto sin truncar retrocompatibilidad con esquemas anteriores.

## Out of scope
- Modificaciones sobre la base temporal (SCD2).
- Creación de gráficos adicionales.
- Pila de peticiones RPC del contador (ya funciona por JIT Cache/DB).

## Done criteria
- La grilla carece de "size" rígidos y adapta uniformemente 4 tarjetas de Dashboard sin dejar huecos de 33% (1 tarjeta sola).
- Entidades configurables pueden pasar Color CSS Code / Tema a través del nodo `uiConfig` explícitamente.
