# Scope S59.2

## In Scope
- Carga condicional del SDK de Google Identity Services en el entorno frontend si estamos en la vista de Workspace.
- Custom rendering del botón OAuth en el `FormRenderer` o en el `DataView_UI` para la entidad `Config_Workspace`.

## Done Criteria
- Renderizado del botón funciona y abre el pop-up nativo de Google.
- Se captura un payload válido de autenticación en el cliente.
