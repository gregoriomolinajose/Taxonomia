# Plan: S45.2 Leader Auto-Provisioning en Cascada

## Tareas

1. **`Business_Interceptors.js`**
   - Crear `AutoProvisionLiderDirecto`
   - Cachear la existencia de emails en O(1).
   - Generar array `batchLideresToCreate`.
   - Incluir fallback para batch inserts atómicos.

2. **`Schema_Engine.js`**
   - Modificar `Persona.mutationInterceptors` para que incluya `AutoProvisionLiderDirecto`.

3. **`Job_WorkspaceSync.js`**
   - Detectar si la persona hidratada (o cualquier persona en el batch) tiene `lider_directo`.
   - Empujar aristas mock `{ tipo_relacion: 'PERSONA_LIDER_DIRECTO', id_nodo_padre: lider, id_nodo_hijo: personaId }` hacia `incomingEdgesMock`.

4. **Validación**
   - Verificar si la UI carga rápidamente.
   - Ejecutar el Job y revisar si el stub muta a un registro real y si se generó un nuevo stub para su jefe.
