# S16.2 Implementation Plan
**Story:** S16.2 Action Router Purification
**Size:** M
**Date:** 01-Apr-2026

## Overview
Decompose `API_Universal.gs` (>500 LOC) into `API_Universal.gs`, `Controller_Action.gs`, and `Controller_Lookups.gs` to comply with Blueprint V4 Single Responsibility patterns.

## Tasks

### Task 1: Create `Controller_Lookups.gs`
- **Description:** Abstract lookup generation functions and cache mechanisms.
- **Files:** `[NEW] src/Controller_Lookups.gs`, `[MODIFY] src/API_Universal.gs`
- **Implementation:** Move `_getCachedLookup`, `getDominioOptions`, `getPersonasOptions`, `getGruposProductosOptions`, `getPortafoliosOptions`, `getProductosOptions`, `getUnidadesNegocioOptions`, `getEquiposOptions` into the new file.
- **Verification:** `node deploy.js dev` parsing check.

### Task 2: Create `Controller_Action.gs`
- **Description:** Abstract core orchestration logic for CUD events and data hydration.
- **Files:** `[NEW] src/Controller_Action.gs`, `[MODIFY] src/API_Universal.gs`
- **Implementation:** Move `_handleCreate`, `_handleRead`, `_handleUpdate`, `_handleDelete`, `getAppBootstrapPayload`, `getInitialPayload`, `bulkInsert`, and `_generateShortUUID`.
- **Verification:** `node deploy.js dev` AST compile check.

### Task 3: API_Universal God-Controller Purification
- **Description:** Clean up `API_Universal.gs` module exports and comments to assert its new identity as a pure Entry_Router.
- **Files:** `[MODIFY] src/API_Universal.gs`
- **Implementation:** Ensure it only delegates `doPost` and `API_Universal_Router` properly. Adjust the CommonJS `module.exports` at the bottom of the files to export the correct units for Jest (local offline tests).
- **Verification:** `node deploy.js dev` should pack the files successfully.

### Task 4: E2E Integration Verification
- **Description:** Manual verification of client-side requests resolving properly against the GAS global context.
- **Verification:** Emulate payload requests using frontend UI or CURL-equivalent against App Script Dev deployment.

## Execution Order
Sequential: Task 1 -> Task 2 -> Task 3 -> Task 4. 
No circular dependencies. High structural risk handled first.
