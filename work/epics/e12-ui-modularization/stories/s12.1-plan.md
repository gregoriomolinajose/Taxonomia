# Plan of Action: S12.1 - FormEngine Splitting

- **Story Size:** L (8+ SP)
- **Date:** 2026-03-31

## Atomic Tasks

1. **T1:** `FormBuilder_Inputs.html` Base Scaffold. (Size: S)
   - *Action:* Create `src/FormBuilder_Inputs.html`. Extrapolate basic native inputs generation (`buildInput`, `buildSelect`, `buildDate`). Return pure Nodes.
   - *Verification:* Script can be appended to Index without syntax errors. `window.UI_Factory` exposes methods.

2. **T2:** Complex Widgets (`buildSearchableMulti`). (Size: M)
   - *Action:* Relocate the topological Multi-select generators resolving `LocalEventBus` injection explicitly.

3. **T3:** Extract `FormValidators.html`. (Size: S)
   - *Action:* Create Validation Script extracting `verifyRequiredFields()`.
   - *Verification:* `window.UI_Validators` evaluates arrays correctly.

4. **T4:** Stripping `FormEngine_UI.html`. (Size: L)
   - *Action:* Delete physical `document.createElement()` trees entirely. Loop over `APP_SCHEMAS[].fields` calling `window.UI_Factory` dynamic builder functions.
   - *Verification:* Zero references to `document.createElement` in the `renderForm` scope.

5. **T5:** Tooling Links & File Rename. (Size: S)
   - *Action:* Move `FormEngine_UI.html` -> `FormRenderer_UI.html`. Update `<script>` references in `Index.html` forcing injection order: Factory -> Validators -> Renderer.
   - *Verification:* `deploy.js` packages the new split correctly.

6. **T6:** E2E UAT. (Size: M)
   - *Action:* Deploy to Dev Sandbox. Trigger a Form with relational constraints (Topology SCD-2). Save it.
   - *Verification:* GC limit test passes. Database correctly persists exact shapes as E11.

## Rationale
Since T4 involves massive deletion, building the external dependencies (T1, T2, T3) statically first is mandatory to prevent intermediate uncompilable states (Risk-First).
