# Scope: S10.2

## In Scope
- Recorte masivo del bloque `<style>` de la línea 7 a la 666 en `DataView_UI.html`.
- Migración y paste idéntico del bloque CSS hacia `CSS_App.html`.
- Refactorización asintótica en `CSS_DesignSystem.html` para el parser recursivo de *Theming* introduciendo el `return;` temprano.

## Out of Scope
- Manipulación de nuevos colores, tipografías o métricas en la interfaz (Es un refactor puramente arquitectónico, no de diseño).
- Construcción de un ThemeManager JavaScript paralelo (Se mantiene en puro CSS nativo según lo evalue el plan).

## Done Criteria
- [ ] Cero coincidencias bajo el regex `<style(.*?)>` dentro del código fuente compilado de `DataView_UI.html`.
- [ ] Inspección del DOM Elements en el navegador no revela variables redundantes tipo `--color-text-default` sino expone una estructura perfectamente aplanada.
