# Scope: S10.1

## In Scope
- Desplazamiento y creación del Objeto de Dominio Constante `UI_Factory` para albergar helpers visuales.
- Mutación de llamados `window._populateSelectOptions` y `window._bindLevelChangeRepaint` hacia `UI_Factory.populateSelectOptions`... en todo `FormEngine_UI.html`.
- Agregado sistemático de comprobaciones `(!Array.isArray(...))` en la firma de las funciones transferidas.

## Out of Scope
- Migración hacia librerías de componentes nativas como React / Vue / Svelte.
- Arquitectura de pruebas explícita Jest sobre dichos modales (Aún no tenemos Jest configurado explícitamente dentro de V8 Sheet Environment, la encomienda aquí es la pureza).

## Done Criteria
- [ ] No existen llamadas a funciones `_populate...` o `_bind...` flotando en `window`.
- [ ] Implementados Guards a pruebas de Array contra payloads vacíos o asíncronos en los helpers encapsulados.
