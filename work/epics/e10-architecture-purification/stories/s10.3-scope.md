# Scope: S10.3

## In Scope
- Programación de una rutina inyectiva en JavaScript puro `window.__runMemoryProfile_E2E(iterations)` escondida dentro de `FormEngine_UI.html` o un componente diagnóstico.
- Auditoría programática para certificar matemáticamente el Garbage Collection reportando al final del loop un console.table() comparando Nodos DOM iniciales versus Finales.
- Adición de un Garbage Collection Handler dentro del `topModal.dismiss()` asegurando que al cerrar el modal todo el HTML se purgue, desvinculando escuchadores de eventos.

## Out of Scope
- Integración de Puppeteer o Cypress externos (la prueba correrá internamente a través del motor del cliente).
- Medición forzada de GPU ni Render Paint (solo nos concierne el Node Count Heap para Evitar Leaks).

## Done Criteria
- [ ] Ejecutar exitosamente el test de 50 iteraciones confirmando que Baseline Nodes == Final Nodes y Heap Memmory liberada.
