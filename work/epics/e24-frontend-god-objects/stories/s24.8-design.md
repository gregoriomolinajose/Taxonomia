# Story Design: S24.8 DataGrid Card View OCP Fix

## What & Why
**Problema:** Tras realizar el `/rai-debug` se concluyó que la constante `HIDDEN_BY_DEFAULT` estaba operando intrínsecamente en `UI_DataGrid.client.js` como una redundancia explícita a la de `DataView_UI`. Esta lógica viola el Open/Closed Principle y causa que la matriz de Tarjetas (Grid View) ignore los estados dinámicos del Popover del Toolbar (Visibility Toggles).
**Valor:** Removeremos la inteligencia de dominio hardcodeada del render de tarjetas de la capa "Tonta" del DataGrid para que éste siga un contrato estricto pasado por su orquestador y respete la elección gráfica del usuario en cualquier vista.

## Approach
Al refactorizar el código se eliminará la constante pre-hardcodeada `HIDDEN_BY_DEFAULT` de la factory `window.UI_DataGrid`.
Modificaremos la función transaccional iteradora `_renderGridView` dentro de `UI_DataGrid.client.js`. Pasaremos de iterar el universo `Object.keys(row)` hacia un bucle seguro fundamentado en `this.cfg.columns.filter(c => c.visible)` garantizando las deduplicaciones.

- **src/UI_DataGrid.client.js** (Modificar): Eliminar la definición constante y mapear los chips de las tarjetas emparejados a las Columnas Pasadas.
- **src/DataView_UI.client.js** (Revisar): Asegurarse de que el constructor `_buildColumns` preserve su lógica base para propagar `visible = false` eficientemente al inicio, sirviendo como SSOT (Single Source of Truth) para las columnas del sistema.
- **__tests__/DataGrid_Integration.ui.test.js** (Nuevo): Crear banco de pruebas JSDOM para certificar el layout de la Grid real.

## Examples
*Comportamiento Anterior (Erróneo):*
1. El usuario tiene `nombre` y `created_at`.
2. Las columnas se derivan para tabla pero el ciclo `Grid` busca `created_at` contra `this.HIDDEN`.
3. El usuario desactiva `nombre` en la barra -> La Tabla borra `nombre` pero la Tarjeta LO MANTIENE visible.

*Comportamiento Esperado (Corregido):*
1. El usuario desactiva la columna `nombre` en el modal de columnas.
2. Tanto la vista de tabla `_renderTableView` como la vista de tarjeta `_renderGridView` consumen la misma estructura purificada `this.cfg.columns`.
3. Las tarjetas renderizan exactamente las métricas requeridas descartando asombros inmutables. 

## Acceptance Criteria
- **MUST:** El array `HIDDEN_BY_DEFAULT` debe de ser borrado por completo purificando a `UI_DataGrid.client.js`.
- **MUST:** `_renderGridView` debe nutrir el contenido de sus tarjetones iterando top 5 de `this.cfg.columns.filter(c => c.visible)` y asociarlos con la fila `row[k.key]`.
- **MUST NOT:** Producirse roturas de `undefined` al iterar diccionarios incompletos originados de uniones débiles en `UI_DataGrid`.
- **MUST:** Implementar una suite de pruebas de integración no mockeada (`DataGrid_Integration.ui.test.js`) contra la fábrica visual para derribar el riesgo de Supervivencia de Mutaciones detectado en el Quality Review.
