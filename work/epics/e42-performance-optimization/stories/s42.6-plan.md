# Story S42.6: Limpieza Quirúrgica de Ghost Records (Rollback Prevencion) - Plan

## Tareas Implementadas

### Tarea 1: Etiquetado Criptográfico
1. Fornicar un Random UUID (ej. `TMP_LOCAL_8FH3K`) y anexarlo a todos los objetos simulados bajo la variable protegida `_optimistic_session` adentro del Payload Optimista en `UI_FormSubmitter`.

### Tarea 2: Destrucción Diferida
1. Inserción de una comprobación de sanidad previa a la Fase 2 (The Background Hydration).
2. Si un registro virtual existe en la tabla y concuerda con un `_sessionId`, el motor `window.DataStore.set()` aplica un filter descartando brutalmente todos los Records de la sesión temporal.
3. Asegurar que las Promise rejection por XHR timeouts disparen invariablemente un `_handleOptimisticRollback()` con un aviso de color rojo Toast a la interfaz.
