# Alcance de S42.1: Save Action Performance Optimization

### In Scope
- Tracking de cascadas de peticiones desde DevTools.
- Identificación de los call sites originales que generan XHR de guardado redundantes / individuales.
- Implementación de debounce/batching a nivel de capa de datos (`AppEventBus` o `DataStore`).

### Out of Scope
- Migración o refactor del modelo de datos backend (mantener las hojas inalteradas, solo agrupar operaciones a nivel frontend o Controller de GAS).

### Done Criteria
- [ ] Tiempos de guardado visibles en DevTools Network drásticamente reducidos.
- [ ] Solución implementada maneja concurrencia y no pierde datos por debounce.
