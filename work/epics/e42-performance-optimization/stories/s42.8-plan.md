# Story S42.8: Desacoplamiento de Reconciliador Front-End (SRP Refactor) - Plan

## Secuencia de Ejecución

### Tarea 1: Creación de Módulo CacheResolver_UI.client.js
1. **Acción:** Crear archivo en la carpeta `/src/` con el envoltorio IIFE estándar para Frontend (`window.CacheResolver_UI = ...`).
2. **Transferencia:** Extraer físicamente el código del método `_patchFrontendCache` del Submitter.

### Tarea 2: Inyección y Refactorización del UI_FormSubmitter
1. **Acción:** Eliminar `_patchFrontendCache` de las entrañas de `UI_FormSubmitter.client.js`.
2. **Sustitución:** Localizar las 2 invocaciones existentes de `this._patchFrontendCache` (en `_attachSubmitListener` fase optimista y background callback).
3. **Reemplazo:** Reemplazarlas por `window.CacheResolver_UI.patchFrontendCache(ent, res, pay)`.

### Tarea 3: Verificación Rígida E2E
1. **Despliegue:** `npm run deploy:dev:auto`.
2. **Ejecución Local:** Esperar luz verde del `appsscript CLI`.
3. **Prueba Operativa:** Navegar en una Entidad "Madre", abrir modal de edicion, cargar Subgrids, click "Guardar", confirmar UI sin bloqueos intermedios y propagación de red (DataGrid Re-render invisible).
