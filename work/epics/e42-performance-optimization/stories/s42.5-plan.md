# Story S42.5: Temporal Graph Edge Indexing (SCD-2 Local) - Plan

## Tareas Implementadas

### Tarea 1: Cimentación del Router Topológico
1. Inserción de la variable `_topologyIndex` en el gestor global `window.DataStore` dentro de `JS_Core.client.js`.
2. Creación del constructor `_buildTopologyIndex()` que itere en `Sys_Graph_Edges` pre-cacheando un mapa Bidimensional en O(1) de la forma `Index[Relación][Padre] = Set(Hijos)`.

### Tarea 2: Sincronización en Mutación
1. Integración de la función `mutateTopologyEdge(edge, isAdd)` para asegurar que cualquier inyección optimista desde el Submitter expanda el Grafo en tiempo y memoria real sin recalcular bucles masivos.
