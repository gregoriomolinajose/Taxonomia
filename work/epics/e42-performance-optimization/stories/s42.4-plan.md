# Story S42.4: Estandarización de Extracción Nodal (Duck-Typing API) - Plan

## Tareas Implementadas

### Tarea 1: Aplicación del Protocolo a Nodos Nativos
1. Adaptar Web Components de alta complejidad (Multiselects, TXSearchable, Dynamic Lists) inyectándoles la función mandatoria de extracción profunda: `this.getValidatedValue = function() { ... }`.

### Tarea 2: Integración al Motor Recolector (FormSubmitter)
1. Sustituir en `UI_FormSubmitter.client.js` la ruda lectura por clases `input[value]` con un colector avanzado y ciego: `document.querySelectorAll('[data-form-component]')`.
2. Validar que la existencia de la función es segura antes de ejecutar, armando el payload en un tiempo ~2ms previniendo Desyncs de DOM.
