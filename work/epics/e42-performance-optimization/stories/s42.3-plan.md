# Story S42.3: Re-Arquitectura de Subgrids Stateless - Plan

## Tareas Implementadas

### Tarea 1: Reescritura del SubgridBuilder
1. Modificación de `UI_SubgridBuilder.client.js` para extraer las referencias globales a variables de RAM central.
2. Purga definitiva del método destructivo `window.DataStore.clearNested()` en `JS_Core.client.js` que colapsaba la hidratación.

### Tarea 2: Filtros de Vista Puros
1. Inyección de lógicas selectivas para la lectura de bordes en `DataStore.getEdgeChildren()`. 
2. Redirección de los componentes Subgrid a leer únicamente de la fuente de la verdad inmutable, sin generar variables intermedias temporales.
