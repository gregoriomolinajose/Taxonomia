# S55.4 Plan: UI de Resumen de Impacto

## Overview
- **Story:** S55.4
- **Size:** M (Moderate)
- **Date:** 2026-05-15

## Tasks

### Task 1: Exponer computeDelta al Cliente
- **Description:** Implementar la lógica pura de `computeDelta` dentro de `window.Graph_Utils` en el archivo `JS_GraphUtils.client.js`.
- **Files:** `src/JS_GraphUtils.client.js`
- **Verification:** Ejecutar `window.Graph_Utils.computeDelta([], [])` en consola sin errores.
- **Size:** XS
- **Dependencies:** Ninguna.

### Task 2: Interceptar Finalización del Wizard
- **Description:** Modificar el método `submitForm` en `UI_FormStepper.client.js` para detectar si el formulario activo es la `Taxonomia`. Si lo es, en lugar de invocar `closeAllSidebars()` directamente, debe recolectar los bordes (edges) y calcular el diff.
- **Files:** `src/UI_FormStepper.client.js`
- **Verification:** Console.log del delta en el momento de hacer click en "Finalizar".
- **Size:** S
- **Dependencies:** Task 1.

### Task 3: Renderizar Modal UI y Resolver Nombres
- **Description:** Crear la función `showDeploymentPreview(delta, onConfirm)` que inyecta un `<ion-modal>` en el DOM con listas verdes (`ion-text-success`) y rojas (`ion-text-danger`). Resolver UUIDs usando `window.DataStore`.
- **Files:** `src/UI_FormStepper.client.js` (o crear nuevo `UI_DeploymentModal.client.js`)
- **Verification:** Modal visible con diseño proporcional, bloquea la UI hasta que se cancela o confirma.
- **Size:** M
- **Dependencies:** Task 2.

## Execution Order
1. **Task 1** - Setup de dependencia lógica.
2. **Task 2** - Enrutamiento del flujo (Intercepción).
3. **Task 3** - UX final y despacho del callback.

## Risks and Mitigations
- **Riesgo:** Resolución de nombres falla si el DataStore no tiene el registro en caché.
  - **Mitigación:** Añadir un `|| 'Desconocido'` robusto al resolver.

## Duration Tracking
| Task | Status | Actual Time |
|------|--------|-------------|
| T1   | Pending|             |
| T2   | Pending|             |
| T3   | Pending|             |
