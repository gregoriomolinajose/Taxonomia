# Story Design: S5.5 - Frontend Treemap Temporal Graph Interceptor

## 1. What & Why
**Problem:** Actualmente `JS_Core.html:buildEChartsTreemapData` lee de la caché plana `Dominio` e intenta utilizar la columna local `id_dominio_padre` para rutear visualmente el mapa de Apache ECharts. Dado el refactor S5.3/S5.4, esta columna está obsoleta y la nueva Topología real reside en `Relacion_Dominios`.
**Value:** Re-escribir el ensamblador visual O(N) para que construya el Grafo con base a la tabla puente, acatando el filtro SCD-2 (`es_version_actual=true`) evitando pintar nodos zombis ni multiplicar a los huérfanos.

## 2. Approach
- Modificar `buildEChartsTreemapData` en `JS_Core.html`:
  1. Construir el diccionario de Entidades (`mapData`) recorriendo `window.__APP_CACHE__['Dominio']`. **Cada nodo es instanciado estáticamente con `value: 1`** garantizando peso simétrico para inflar y estabilizar cajas vacías en el renderizado móvil de ECharts.
  2. Extraer `window.__APP_CACHE__['Relacion_Dominios']`.
  3. Ejecutar filtro en memoria: `edge.es_version_actual === true || edge.es_version_actual === "TRUE"` y `edge.tipo_relacion === 'Militar_Directa'`.
  4. Crear un array `hasParentMap = {}`.
  5. Recorrer aristas filtradas. Por cada una, `mapData[edge.padre].children.push(mapData[edge.hijo])` y setear `hasParentMap[edge.hijo] = true;`.
  6. Recorrer `mapData`. Todos los nodos donde `!hasParentMap[id]` son volcados a `rootNodes`.
- El Treemap ECharts continuará consumiendo `rootNodes` sin enterarse de que provienen de una inyección de Grafo Dirigido Acíclico en vez de un modelo plano.

## 3. Concrete Examples
### Ejecución
- Entorno: 3 Dominios (A, B, C). 2 Relaciones (A->B activa, A->C caducada, B->C activa).
- El filtro descarta A->C (SCD-2).
- Se ancla C a B, y B a A.
- A es root.

## 4. Acceptance Criteria
- **MUST:** No deben graficarse Nodos duplicados en diferentes Ramas bajo ningún concepto.
- **MUST:** El código ECharts `value = 1` debe mantenerse para no causar fallos de renderizado en UI móvil.
- **MUST:** El código debe ser a prueba de fallos si la tabla `Relacion_Dominios` aún está vacía (Fallback seguro).
