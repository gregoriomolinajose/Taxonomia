# Scope: S5.6

**In Scope:**
- Integración de `Relacion_Dominios` a las capas lógicas del UI cliente.
- Refactor de `buildOrdenPath` y posibles repercusiones en `Math_Engine.js`.
- Refactor de inyección asíncrona (`openEditForm` o análogo en JS client) para pre-poblar listboxes.

**Out of Scope:**
- Componentes backend. La tabla puente ya fue implementada y bajada mediante `__APP_CACHE__` en S5.5.
- Cambios a librerías D3/ECharts (Resueltas en S5.5).

**Done criteria:**
- Edición de Dominio existente no arroja campo "Dominio Padre" vacío si posee un enlace válido.
- Path Name y Path Orden en el formulario continúan computándose de manera determinista.
