# S5.6: Refactor Frontend DAG Agnosticism

## Implementation Plan

### Task 1: Schema Modifications
**Component:** `src/Schema_Engine.gs`
**Action:** 
1. Remover (Eliminar) el campo de interfaz transient `id_nodo_padre`.
2. Remover (Eliminar) los campos autocalculados `orden_path` y `path_completo_es`.
3. Inyectar (Añadir) un nuevo campo de Subgrid Relacional que vincule matricialmente a los padres:
```javascript
{ section: "Topología (Grafo)", width: 12, name: "relaciones_padre", type: "relation", relationType: "1:N", targetEntity: "Relacion_Dominios", foreignKey: "id_nodo_hijo", uiBehavior: "subgrid", label: "Administrar Padres (N:M)" }
```

### Task 2: Backend Orchestration Cleanup
**Component:** `src/Engine_DB.js`
**Action:**
1. Desmantelar la inyección transitoria singular (El bloque que extirpaba `transientParentId` y llamaba a `_updateGraphEdges()`).
2. Al declararse un *subgrid* hacia `Relacion_Dominios`, la rutina nativa `orchestrateNestedSave` asumirá mecánicamente la validación del arreglo.
3. Actualizar `_flattenGraphNode` o la lógica SCD-2 para que las inyecciones de `orchestrateNestedSave` validen `es_version_actual` al mutar las aristas (puesto que el subgrid reemplazará la tabla puente directamente). (Wait, el motor `orchestrateNestedSave` hace Hard Deletes de hijos; si queremos preservar SCD-2 para relaciones de Dominio, el Engine_DB debe interceptar el batch de `orchestrateNestedSave` si el child es `Relacion_Dominios`).

### Task 3: FormEngine UI Ajustes
**Component:** `src/FormEngine_UI.html`
**Action:** 
1. Eliminar trazas legacy de callbacks matemáticos `getGenericOrdenPath` que pudiesen crashear, o simplemente dejarlos huérfanos sin causar error. El schema los dejará inactivos.

## Validation Steps
- Al correr `FormEngine_UI`, al crear o editar Dominio, no deben aparecer los inputs lockeados de Path ni Orden.
- Debe renderizarse un subgrid para crear Registros puente de `Relacion_Dominios`.
