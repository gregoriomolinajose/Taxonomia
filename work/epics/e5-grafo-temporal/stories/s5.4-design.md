# Story Design: S5.4 - Algoritmo de Cascade Flattening

## 1. What & Why
**Problem:** Actualmente, `API.delete("Dominio", id)` invoca el borrado de la tabla física plana, pero no tiene conciencia del Grafo. Esto deja aristas "zombis" en `Relacion_Dominios` y genera nodos huérfanos sin heredar el linaje de sus superiores.
**Value:** Desarrollar un "Soft-Delete Topológico" o "Cascade Flattening" que evada la orfandad re-asignando a los nietos la custodia de su abuelo directo, un requerimiento clave en las Taxonomías Analíticas Dinámicas.

## 2. Approach
- **Interceptor en `delete()` de `Engine_DB.js`:** 
  1. Si `entityName === "Dominio"`, detener temporalmente el borrado físico y ejecutar un pre-vuelo `_flattenGraphNode(nodeIdToDelete, config)`.
  2. En `_flattenGraphNode`:
     - Cargar toda la matriz `Relacion_Dominios`.
     - Identificar el **Arista Superior** (donde `hijo == nodeIdToDelete` y `version_actual == true`). Obtener el `Abuelo_ID`. Cortar / Expilar esa Arista.
     - Identificar todas las **Aristas Inferiores** (dónde `padre == nodeIdToDelete` y `version_actual == true`). Éstos son los **Nietos**. Por cada arista, expilarla.
     - Iterar sobre la lista de **Nietos**, e instanciar en RAM Aristas nuevas uniendo `Abuelo_ID -> Nieto_ID`.
     - *Batch Write* a la nube y proceder con `_Adapter_Sheets.remove(nodeIdToDelete)`.

- **Componentes:** `src/Engine_DB.js`

## 3. Concrete Examples
### Ejecución
- Nodos Existentes: ESTRATEGIA (Padre), OPERATIVA (Hijo), TACTICA (Nieto).
- Arista A: ESTRATEGIA -> OPERATIVA
- Arista B: OPERATIVA -> TACTICA
- El humano borra **OPERATIVA**.
- El script extrae al Abuelo (ESTRATEGIA). Extrae al Nieto (TACTICA).
- El Script cancela SCD-2 de Arista A y B.
- El Script forja C: ESTRATEGIA -> TACTICA.

## 4. Acceptance Criteria
- **MUST:** Ejecutar el saneamiento completo en O(N) interactuando con Sheet API UNA SOLA VEZ mediante Bulk Arrays.
- **MUST:** Respetar la nomenclatura Universal `RELA-XXXXX` para las nuevas asignaciones temporales.
- **MUST:** Ejecutar `_invalidateCache` tras la supresión para forzar a FormEngine a rearmar el Combobox Topológico.
