# Design S58.1: Actualización de Esquemas Estructurales (Rol + Entidades)

## Architecture
- Modificaremos directamente el objeto literal `APP_SCHEMAS` en `src/Schema_Engine.js`.
- Utilizaremos componentes nativos del schema engine (`select`, `text`, `relation` con `searchable_single`).

## Components Affected
- `src/Schema_Engine.js` -> Objetos: `Rol`, `Value_Stream`, `Portafolio`, `Grupo_Productos`, `Equipo`.
