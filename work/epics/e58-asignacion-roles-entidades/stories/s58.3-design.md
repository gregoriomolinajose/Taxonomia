# Design S58.3: Aislamiento Topológico

## Modificaciones Estructurales

1. `Engine_Graph.js (analyzeTopology)`:
```javascript
// Construcción de diccionarios
parentsOf[childId].push({ id: pId, type: e.tipo_relacion });
childrenOf[pId].push({ id: childId, type: e.tipo_relacion });

// DFS de ascendencia (maxDepth y cycles)
for (const p of nextParents) {
    if (p.type !== targetRelationType) continue;
    const res = dfsTraversal(p.id, pathStack, memo);
    ...
}

// DFS de descendencia (getSubtreeMaxDepth)
childrenOf[nodeId].forEach(c => {
    if (c.type !== targetRelationType) return;
    if (!visitedDown.has(c.id)) { ... }
});
```
*`targetRelationType` se inyecta pasándolo a `dfsTraversal` tomando `newEdge.tipo_relacion`.*

2. `Schema_Engine.js`:
En `TOPOLOGY_PRESETS.JERARQUICA_PERSONA`, se añade explícitamente `maxDepth: 15`.
