# Plan S58.1: Actualización de Esquemas Estructurales

## Tareas
- [ ] Modificar esquema `Rol` (agregar `nombre_ingles`, `color_icono`, ajustar `especialidad`).
- [ ] Agregar `dueno_vs_id` en `Value_Stream`.
- [ ] Agregar `gerente_portafolio_id` en `Portafolio`.
- [ ] Agregar `gerente_producto_id` en `Grupo_Productos`.
- [ ] Renombrar `product_owner_id` en `Equipo`.
- [ ] Agregar `AutoProvisionEntityRoles` a `mutationInterceptors` en las 4 entidades.
