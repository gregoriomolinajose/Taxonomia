# Scope S58.3: Aislamiento Topológico

## In Scope
- Actualización de `parentsOf` y `childrenOf` en `Engine_Graph.js` para almacenar la tupla `{ id, type }`.
- Filtrado durante `dfsTraversal` y `getSubtreeMaxDepth` usando el `tipo_relacion`.
- Configuración de `maxDepth: 15` para Persona en `Schema_Engine.js`.

## Out of Scope
- Migración de datos o limpieza de aristas existentes.
- Refactorización mayor del `Engine_Graph.js` más allá de estas correcciones quirúrgicas.
