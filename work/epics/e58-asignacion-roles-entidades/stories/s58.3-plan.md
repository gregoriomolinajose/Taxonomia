# Plan S58.3: Aislamiento Topológico

## Tareas
- [ ] Modificar `Schema_Engine.js` -> `TOPOLOGY_PRESETS.JERARQUICA_PERSONA` (`maxDepth: 15`).
- [ ] Modificar `Engine_Graph.js` -> Llenado de `parentsOf`/`childrenOf` para usar objetos `{ id, type }`.
- [ ] Modificar `Engine_Graph.js` -> `dfsTraversal` para saltar aristas con distinto `type`.
- [ ] Modificar `Engine_Graph.js` -> `getSubtreeMaxDepth` para saltar aristas con distinto `type`.
- [ ] Actualizar recolección de `parentsOf`/`childrenOf` en la sección 4 (Orphan Stealing Check).
- [ ] Realizar despliegue a `dev`.
