# Scope S58.1: Actualización de Esquemas Estructurales (Rol + Entidades)

## In Scope
- Actualización del esquema `Rol` en `Schema_Engine.js` (añadir `nombre_ingles`, `color_icono`, ajustar `especialidad`).
- Adición de campos de rol específicos (ej. `dueno_vs_id`) a `Value_Stream`, `Portafolio`, `Grupo_Productos` y renombrado en `Equipo`.
- Adición del interceptor `AutoProvisionEntityRoles` a `mutationInterceptors` de estas entidades.

## Out of Scope
- Implementación del interceptor `AutoProvisionEntityRoles` en `Business_Interceptors.js` (esto pertenece a S58.2).
- Migración de datos históricos.

## Done Criteria
- `Schema_Engine.js` refleja exactamente los requerimientos visuales y estructurales acordados.
- Las vistas de la interfaz se renderizan correctamente con los nuevos campos de roles apuntando a `Persona`.
