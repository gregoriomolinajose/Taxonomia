# Retrospectiva S22.3

## Entregables Logrados
- Tablas sistémicas expulsadas del alcance operativo modificando la Verdad Única (`Schema_Engine.gs`).
- Elementos residuales reacomodados en S22.1 bajo blindaje corporativo (ABAC).
- Eliminación de la caja de Bienvenida text-heavy en `Index.html`.

## Calidad
1. **Single Source of Truth:** Excelente patrón de purga UI. Al mutar `showInMenu: false` en `Schema_Engine.gs`, el problema se mitigó automáticamente en todo lugar que lea `APP_SCHEMAS`.

## Lecciones Deducidas
- No debemos usar `if(entityName == 'Sys_XXX')` en el front. Siempre es mejor extender los metadatos desde Google Sheets/Backend hacia JS.
