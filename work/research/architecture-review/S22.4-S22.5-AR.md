## Architecture Review: S22.4 & S22.5 (V2 - Refactored)

### Contexto
Tercera revisión después de aplicar las recomendaciones (S22.4, S22.5 y Backend `Adapter_Sheets.js`).

### Critical (fix before merge)
*Ninguno.*

### Recommended (simplify before next cycle)
*Ninguno.* 

### Questions (require human judgment)
*Ninguna.* Las observaciones previas fueron subsanadas en su totalidad. 

### Observations (patterns noted)
- **Data-Driven UI:** La inyección de la Bottom Tab Bar ahora utiliza el arreglo local `thumbZoneConfig`, que resuelve la advertencia de Indirection Depth (H6) y de Semantic Duplication (H9), preparando el terreno para una eventual sincronización con un servidor de configuraciones ABAC si se requiriera en el futuro.
- **Tolerancia a Multi-Bandera (Adapter_Sheets.js):** Se introdujo el control de Soft-Delete por compatibilidad cruzada (`deleted_at`, `estado` y el metadato nativo de Apps Script `_isDeleted`). Esta heurística cumple con la regla de proporcionalidad (KISS): consolida 3 vías de baja en el menor número de abstracciones lógicas posible (usando un sólo método `_isNodeLogicallyDeleted` de 5 líneas de código).

### Verdict
- [x] PASS
