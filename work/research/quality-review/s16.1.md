## Quality Review: S16.1 (Metadata SSOT Refactoring)

### Critical (fix before merge)
No existen errores críticos. El motor `Schema_Engine.gs` es semánticamente sólido y el inyector topológico del Frontend genera listas seguras sin falsys. Toda la validación principal, incluyendo el AST, pasó exitosamente.

### Recommended (improve code quality)
1. **Semantic Muda - Redundant Branching (JS Idiom):**
   - *File:* `Dashboard_UI.html (L109)`, `UI_Router.html (L101)`
   - *Why:* Después de implementar la nueva abstracción `window.getEntitiesByMetaFlag(flagName)` en la que el iterador ya filtra explícitamente `(meta && meta[flagName] === true)`, dentro del `.map()` subsecuente en las vistas sigues comprobando `if(meta.showInDashboard === false) return '';`. 
   - *Fix:* Eliminar esta compuerta estática (`if(meta.showInDashboard === false) return '';` y `if(meta.showInMenu === false) return '';`). Es código muerto ("Muda") ya que el Helper garantiza matemáticamente que ese condicional jamás se va a cumplir.

2. **Semantic Safety - Truthiness Traps en Ordenamiento (JS Idiom):**
   - *File:* `Index.html (L71)`
   - *Why:* En `Object.entries(items).sort(function(a,b){ return a[1].order - b[1].order; })`. Si mañana un desarrollador agrega una entidad nueva en `Schema_Engine.gs` pero olvida incluir el parámetro `order` en `metadata`, la matemática se truncará en `undefined - undefined = NaN`, lo cual corromperá la predictibilidad del Sorting Array en V8.
   - *Fix:* Garantizar caída segura (Fall-through): `return (a[1].order || 999) - (b[1].order || 999);`

### Observations (no action needed)
- **Extracción limpia:** Has respetado perfectamente la integridad del objeto `window.APP_SCHEMAS`. La variable `ENTITY_META` residual que sigue alimentando a algunas dependencias viejas nunca interfiere con tus nuevas construcciones.
- Las constantes ES6 (como `Icon` o `Color`) no están sufriendo colusiones con palabras clave reservadas de la capa HTML.

### Verdict
- [x] PASS WITH RECOMMENDATIONS
