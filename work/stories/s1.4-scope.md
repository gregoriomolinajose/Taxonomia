# Scope: Hito S1.4 - Sidebar Floating Card

## In Scope
- Inject CSS in `CSS_App.html` for `ion-menu::part(container)`.
- Reassign variables to tokens (`--rounded-md`, `--spacing-4`, `--shadow-floating`).
- Add specific tweaks for Dark Mode shadow variation in `CSS_DesignSystem.html`.
- Maintain `.shell-mini` responsive margin boundaries.

## Out of Scope
- Modifying DOM elements in `Index.html` or the Sidebar TS/JS logic.
- Adding arbitrary animations.

## Done when:
- Deploy to Dev validates the visual layout locally.
- Story constraints are reviewed and accepted by UI rules.
